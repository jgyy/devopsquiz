[
  {
    "id": "kubernetes-001",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the smallest deployable unit that can be created and managed directly in Kubernetes?",
    "options": [
      "A Pod",
      "A Container",
      "A Node",
      "A Deployment"
    ],
    "answer": 0,
    "explanation": "A Pod is the smallest deployable unit in Kubernetes; it wraps one or more containers that share network and storage. Containers cannot be scheduled directly, and Nodes and Deployments are higher-level concepts.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/pods/",
    "tags": [
      "pods",
      "fundamentals"
    ]
  },
  {
    "id": "kubernetes-002",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "boolean",
    "prompt": "Containers that belong to the same Pod share the same network namespace, so they can reach each other over `localhost`.",
    "answer": true,
    "explanation": "All containers in a Pod share the Pod's network namespace (and IP address), so they communicate via `localhost`, and they can optionally share storage through mounted volumes.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/pods/",
    "tags": [
      "pods",
      "networking"
    ]
  },
  {
    "id": "kubernetes-003",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the primary purpose of a `ReplicaSet`?",
    "options": [
      "To ensure a specified number of identical Pod replicas are running at all times",
      "To expose a set of Pods as a network service",
      "To store configuration data for Pods",
      "To schedule Pods onto specific nodes"
    ],
    "answer": 0,
    "explanation": "A ReplicaSet's purpose is to maintain a stable set of replica Pods running at any given time, creating or removing Pods as needed to match the desired count.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/",
    "tags": [
      "replicaset"
    ]
  },
  {
    "id": "kubernetes-004",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "How does a `Deployment` improve on managing Pods with a bare `ReplicaSet` directly?",
    "options": [
      "It adds declarative rolling updates, rollback, and revision history on top of ReplicaSet management",
      "It removes the need for a ReplicaSet entirely",
      "It allows Pods to run without a container image",
      "It replaces the need for a scheduler"
    ],
    "answer": 0,
    "explanation": "A Deployment manages ReplicaSets for you and adds features like declarative rolling updates, pausing/resuming rollouts, and rolling back to a previous revision — capabilities a bare ReplicaSet does not provide on its own.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/",
    "tags": [
      "deployment",
      "replicaset"
    ]
  },
  {
    "id": "kubernetes-005",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "A StatefulSet named `web` with 3 replicas creates Pods with which naming pattern?",
    "options": [
      "web-0, web-1, web-2",
      "web-a, web-b, web-c",
      "web-pod1, web-pod2, web-pod3",
      "Randomly generated names, just like a ReplicaSet"
    ],
    "answer": 0,
    "explanation": "StatefulSet Pods get a sticky, unique ordinal index appended to the StatefulSet name, producing predictable names such as `web-0`, `web-1`, `web-2`, which persist across rescheduling.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/",
    "tags": [
      "statefulset"
    ]
  },
  {
    "id": "kubernetes-006",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "A StatefulSet must be paired with a headless Service (`clusterIP: None`) to provide the stable network identity for its Pods.",
    "answer": true,
    "explanation": "StatefulSets rely on a governing headless Service to give each Pod a stable, predictable DNS name (for example `web-0.web.default.svc.cluster.local`); without it, the Pods lack this stable network identity.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/",
    "tags": [
      "statefulset",
      "dns"
    ]
  },
  {
    "id": "kubernetes-007",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What does a `DaemonSet` guarantee?",
    "options": [
      "That a copy of a Pod runs on all (or a selected subset of) nodes in the cluster",
      "That exactly one Pod runs in the entire cluster",
      "That Pods are automatically scaled based on CPU usage",
      "That a Pod restarts every time it completes successfully"
    ],
    "answer": 0,
    "explanation": "A DaemonSet ensures that all (or some, via node selectors/affinity) nodes run a copy of a Pod — commonly used for node-level agents like log collectors or monitoring daemons.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/",
    "tags": [
      "daemonset"
    ]
  },
  {
    "id": "kubernetes-008",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "Which statement correctly describes a Kubernetes `Job`?",
    "options": [
      "It creates one or more Pods and ensures a specified number of them terminate successfully; its Pod template's `restartPolicy` must be `OnFailure` or `Never`",
      "It runs Pods on a repeating cron schedule",
      "It keeps a Pod running forever, restarting it with `restartPolicy: Always`",
      "It exposes a stable virtual IP for a set of Pods"
    ],
    "answer": 0,
    "explanation": "A Job creates Pods and tracks successful completions; because a Job's Pods are expected to terminate, the Pod template cannot use `restartPolicy: Always` — only `OnFailure` or `Never` are allowed. Running Pods on a repeating schedule is instead the job of a CronJob.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/job/",
    "tags": [
      "job"
    ]
  },
  {
    "id": "kubernetes-009",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "fill",
    "prompt": "Write the standard 5-field cron schedule expression that runs a `CronJob` once every day at midnight (00:00).",
    "answer": [
      "0 0 * * *"
    ],
    "explanation": "CronJob schedules use the standard crontab format `minute hour day-of-month month day-of-week`. `0 0 * * *` means minute 0, hour 0, every day, every month, every weekday — i.e. midnight daily.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/",
    "tags": [
      "cronjob",
      "cron"
    ],
    "pattern": "^0\\s+0\\s+\\*\\s+\\*\\s+\\*$"
  },
  {
    "id": "kubernetes-010",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "If you create a Service without specifying `type`, what type does it default to?",
    "options": [
      "ClusterIP",
      "NodePort",
      "LoadBalancer",
      "ExternalName"
    ],
    "answer": 0,
    "explanation": "`ClusterIP` is the default Service type; it exposes the Service on an internal IP reachable only from within the cluster.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/service/",
    "tags": [
      "service"
    ]
  },
  {
    "id": "kubernetes-011",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "By default, what port range does Kubernetes allocate for `NodePort` Services?",
    "options": [
      "30000-32767",
      "1024-2048",
      "8000-9000",
      "20000-30000"
    ],
    "answer": 0,
    "explanation": "Unless the API server's `--service-node-port-range` flag is changed, NodePort Services are assigned a port from the default range 30000-32767 on every node.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/service/",
    "tags": [
      "service",
      "nodeport"
    ]
  },
  {
    "id": "kubernetes-012",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of the following are valid built-in `type` values for a Kubernetes Service? (Select all that apply.)",
    "options": [
      "ClusterIP",
      "NodePort",
      "LoadBalancer",
      "ExternalName",
      "Ingress",
      "Gateway"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "The four built-in Service types are `ClusterIP`, `NodePort`, `LoadBalancer`, and `ExternalName`. `Ingress` and `Gateway` are separate API objects for routing HTTP(S) traffic, not Service types.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/service/",
    "tags": [
      "service",
      "types"
    ]
  },
  {
    "id": "kubernetes-013",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "You create a Service with `type: LoadBalancer` on a self-managed cluster with no cloud-controller-manager or load-balancer integration configured. What happens?",
    "options": [
      "The Service's external IP stays `<pending>`, and it otherwise behaves like a NodePort Service",
      "Kubernetes automatically provisions a software load balancer using kube-proxy",
      "The Service creation is rejected by the API server",
      "The Service silently becomes a ClusterIP Service"
    ],
    "answer": 0,
    "explanation": "Without a controller that can provision an external load balancer, a `LoadBalancer` Service simply never gets an external IP assigned (it stays `<pending>`), while still receiving the NodePort and ClusterIP allocations a LoadBalancer Service also gets.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/service/",
    "tags": [
      "service",
      "loadbalancer"
    ]
  },
  {
    "id": "kubernetes-014",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "What is the effect of setting `clusterIP: None` on a Service (a 'headless' Service)?",
    "options": [
      "DNS queries for the Service return the individual Pod IPs directly instead of a single virtual cluster IP",
      "The Service is disabled and stops routing traffic",
      "The Service is assigned a new random internal IP on every query",
      "All traffic to the Service is dropped until an Ingress is created"
    ],
    "answer": 0,
    "explanation": "A headless Service has no cluster IP; DNS lookups for it return the set of individual Pod IPs (or per-Pod DNS records for a StatefulSet) instead of load-balancing through a single virtual IP, letting clients discover and connect to individual Pods directly.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/service/",
    "tags": [
      "service",
      "dns",
      "headless"
    ]
  },
  {
    "id": "kubernetes-015",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the primary purpose of an `Ingress` resource in Kubernetes?",
    "options": [
      "To expose HTTP and HTTPS routes from outside the cluster to Services inside it, based on host and path rules",
      "To store TLS certificates for Pods",
      "To assign static IP addresses to Pods",
      "To restrict network traffic between Pods"
    ],
    "answer": 0,
    "explanation": "Ingress defines rules for routing external HTTP/HTTPS traffic to internal Services, typically based on hostname and URL path, and can also terminate TLS.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/ingress/",
    "tags": [
      "ingress"
    ]
  },
  {
    "id": "kubernetes-016",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "Creating an `Ingress` resource has no routing effect unless an Ingress controller is also running in the cluster to implement it.",
    "answer": true,
    "explanation": "Kubernetes does not ship a built-in Ingress controller; the Ingress resource is just a set of rules, and an Ingress controller (for example ingress-nginx or a cloud provider's controller) must be deployed to actually watch and fulfil those rules.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/ingress-controllers/",
    "tags": [
      "ingress",
      "ingress-controller"
    ]
  },
  {
    "id": "kubernetes-017",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is a `ConfigMap` primarily used for?",
    "options": [
      "Storing non-confidential configuration data as key-value pairs that Pods can consume",
      "Storing TLS certificates and passwords securely",
      "Defining RBAC permissions for a namespace",
      "Persisting data across Pod restarts like a database"
    ],
    "answer": 0,
    "explanation": "ConfigMaps hold non-sensitive configuration data (such as settings, file contents, or CLI arguments) that can be injected into Pods as environment variables, command-line arguments, or mounted files. Sensitive data belongs in a `Secret` instead.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/configmap/",
    "tags": [
      "configmap"
    ]
  },
  {
    "id": "kubernetes-018",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "By default, Kubernetes `Secret` values are only base64-encoded, not encrypted, when stored in etcd.",
    "answer": true,
    "explanation": "Base64 is an encoding, not encryption — anyone with API or etcd access can trivially decode it. Encryption at rest requires explicitly configuring an `EncryptionConfiguration` (for example with a KMS provider) for the API server.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/secret/",
    "tags": [
      "secret",
      "security"
    ]
  },
  {
    "id": "kubernetes-019",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of the following are valid ways for a Pod to consume data from a `ConfigMap`? (Select all that apply.)",
    "options": [
      "As individual environment variables via `valueFrom.configMapKeyRef`",
      "As a full set of environment variables via `envFrom`",
      "As files in a mounted volume",
      "As the container's base image",
      "As the Pod's `restartPolicy`"
    ],
    "answer": [
      0,
      1,
      2
    ],
    "explanation": "ConfigMap data can populate individual environment variables, be bulk-imported with `envFrom`, or be mounted as files in a volume. A ConfigMap cannot supply a container image or a `restartPolicy`.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/configmap/",
    "tags": [
      "configmap"
    ]
  },
  {
    "id": "kubernetes-020",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "Which built-in Secret type is designed to hold credentials for pulling images from a private container registry?",
    "options": [
      "kubernetes.io/dockerconfigjson",
      "kubernetes.io/tls",
      "Opaque",
      "kubernetes.io/service-account-token"
    ],
    "answer": 0,
    "explanation": "`kubernetes.io/dockerconfigjson` stores a Docker config file used for authenticating to private registries, referenced from a Pod via `imagePullSecrets`. `kubernetes.io/tls` holds certificate/key pairs, `Opaque` is the generic default type, and `kubernetes.io/service-account-token` holds a ServiceAccount's token.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/secret/",
    "tags": [
      "secret",
      "registry"
    ]
  },
  {
    "id": "kubernetes-021",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What does a `PersistentVolumeClaim` (PVC) represent?",
    "options": [
      "A user's request for storage, which Kubernetes binds to a matching PersistentVolume",
      "A physical disk attached directly to a node",
      "A snapshot of an existing volume",
      "A StorageClass definition"
    ],
    "answer": 0,
    "explanation": "A PVC is a request for storage made by a user (or Pod); Kubernetes binds it to a `PersistentVolume` that satisfies its size, access mode, and (optionally) StorageClass requirements.",
    "reference": "https://kubernetes.io/docs/concepts/storage/persistent-volumes/",
    "tags": [
      "pvc",
      "storage"
    ]
  },
  {
    "id": "kubernetes-022",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What is the main purpose of a `StorageClass`?",
    "options": [
      "To enable dynamic provisioning of PersistentVolumes with a given set of parameters",
      "To encrypt data written to any volume",
      "To limit the number of Pods per node",
      "To define network policies for storage traffic"
    ],
    "answer": 0,
    "explanation": "A StorageClass describes a 'class' of storage (provisioner, parameters, reclaim policy) that lets the cluster automatically (dynamically) provision a PersistentVolume when a matching PVC is created, instead of requiring an administrator to pre-create volumes.",
    "reference": "https://kubernetes.io/docs/concepts/storage/storage-classes/",
    "tags": [
      "storageclass",
      "storage"
    ]
  },
  {
    "id": "kubernetes-023",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "A `PersistentVolume` is a cluster-scoped resource (not namespaced), while a `PersistentVolumeClaim` that uses it is namespaced.",
    "answer": true,
    "explanation": "PersistentVolumes are cluster-level resources like Nodes, visible cluster-wide, whereas PersistentVolumeClaims live in a namespace and can only be used by Pods in that same namespace.",
    "reference": "https://kubernetes.io/docs/concepts/storage/persistent-volumes/",
    "tags": [
      "pv",
      "pvc",
      "storage"
    ]
  },
  {
    "id": "kubernetes-024",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "multi",
    "prompt": "Which of these are valid PersistentVolume access modes? (Select all that apply.)",
    "options": [
      "ReadWriteOnce",
      "ReadOnlyMany",
      "ReadWriteMany",
      "ReadWriteOncePod",
      "ReadWriteAny"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "Kubernetes defines `ReadWriteOnce` (read-write by a single node), `ReadOnlyMany`, `ReadWriteMany`, and the newer `ReadWriteOncePod` (read-write by a single Pod cluster-wide). `ReadWriteAny` is not a real access mode.",
    "reference": "https://kubernetes.io/docs/concepts/storage/persistent-volumes/",
    "tags": [
      "pv",
      "access-modes"
    ]
  },
  {
    "id": "kubernetes-025",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the main purpose of a Kubernetes `Namespace`?",
    "options": [
      "To provide a mechanism for isolating and scoping groups of resources within a single cluster",
      "To physically separate clusters onto different hardware",
      "To define network firewall rules between Pods",
      "To limit which container images can be pulled"
    ],
    "answer": 0,
    "explanation": "Namespaces divide a single cluster into multiple virtual clusters, scoping resource names and letting teams or environments share a cluster without name collisions. They do not provide network isolation by themselves.",
    "reference": "https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/",
    "tags": [
      "namespace"
    ]
  },
  {
    "id": "kubernetes-026",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "fill",
    "prompt": "When you create a resource without specifying a namespace, and your kubeconfig context sets none, which namespace is it created in?",
    "answer": [
      "default"
    ],
    "explanation": "Kubernetes provides an initial namespace called `default` for objects that don't specify one.",
    "reference": "https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/",
    "tags": [
      "namespace"
    ],
    "pattern": "^default$"
  },
  {
    "id": "kubernetes-027",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What is the key difference between a `Role` and a `ClusterRole` in RBAC?",
    "options": [
      "A `Role` grants permissions within a single namespace, while a `ClusterRole` grants permissions cluster-wide (or reusable across namespaces)",
      "A `Role` can only be bound to ServiceAccounts, while a `ClusterRole` can only be bound to users",
      "A `Role` controls network access, while a `ClusterRole` controls storage access",
      "There is no real difference; they are aliases for the same object"
    ],
    "answer": 0,
    "explanation": "A `Role` is namespaced and its rules only apply within that namespace, whereas a `ClusterRole` is a cluster-scoped resource that can grant access to cluster-scoped resources or be reused across multiple namespaces.",
    "reference": "https://kubernetes.io/docs/reference/access-authn-authz/rbac/",
    "tags": [
      "rbac",
      "role"
    ]
  },
  {
    "id": "kubernetes-028",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "How does a `LimitRange` differ from a `ResourceQuota` in a namespace?",
    "options": [
      "A `LimitRange` sets default/min/max resource constraints per Pod or container, while a `ResourceQuota` caps the total aggregate resource consumption across the whole namespace",
      "A `LimitRange` applies cluster-wide while `ResourceQuota` applies only per Pod",
      "A `LimitRange` and a `ResourceQuota` are two names for the same object",
      "A `LimitRange` restricts the number of Secrets, while `ResourceQuota` restricts CPU only"
    ],
    "answer": 0,
    "explanation": "`LimitRange` constrains individual Pods/containers (for example default requests/limits, or min/max), while `ResourceQuota` constrains the sum total of resources (CPU, memory, object counts, and so on) consumed by everything in a namespace.",
    "reference": "https://kubernetes.io/docs/concepts/policy/resource-quotas/",
    "tags": [
      "rbac",
      "resourcequota",
      "limitrange"
    ]
  },
  {
    "id": "kubernetes-029",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the purpose of a `ServiceAccount` in Kubernetes?",
    "options": [
      "To provide an identity that processes running inside a Pod use to authenticate to the Kubernetes API server",
      "To provide login credentials for human users of `kubectl`",
      "To store a Pod's environment variables",
      "To define which node a Pod should run on"
    ],
    "answer": 0,
    "explanation": "ServiceAccounts give an identity to processes running in Pods so they can authenticate API requests; they are distinct from the user accounts humans use to access the cluster.",
    "reference": "https://kubernetes.io/docs/concepts/security/service-accounts/",
    "tags": [
      "rbac",
      "serviceaccount"
    ]
  },
  {
    "id": "kubernetes-030",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "fill",
    "prompt": "Which two-word `kubectl` subcommand pair lets you check whether an identity is allowed to perform a given action, as in `kubectl ___ create pods --namespace dev`?",
    "answer": [
      "auth can-i",
      "kubectl auth can-i"
    ],
    "explanation": "`kubectl auth can-i <verb> <resource>` queries the API server's authorization layer (via a SelfSubjectAccessReview) to report whether the acting identity is permitted to perform that action.",
    "reference": "https://kubernetes.io/docs/reference/access-authn-authz/authorization/",
    "tags": [
      "rbac",
      "kubectl"
    ],
    "pattern": "^(kubectl\\s+)?auth\\s+can-i$"
  },
  {
    "id": "kubernetes-031",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What happens when a container's liveness probe fails repeatedly (past its failure threshold)?",
    "options": [
      "The kubelet kills and restarts the container according to the Pod's restart policy",
      "The Pod is immediately deleted and rescheduled to another node",
      "The container is removed from Service endpoints but left running",
      "Nothing happens; liveness probes are only used for monitoring dashboards"
    ],
    "answer": 0,
    "explanation": "A failing liveness probe tells the kubelet the container is unhealthy, so it kills the container and restarts it (subject to the Pod's `restartPolicy`). This differs from a readiness probe, which only affects Service endpoint membership.",
    "reference": "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/",
    "tags": [
      "probes",
      "liveness"
    ]
  },
  {
    "id": "kubernetes-032",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What is the effect of a container's readiness probe failing?",
    "options": [
      "The Pod's IP is removed from the endpoints of any Service that selects it, without restarting the container",
      "The container is immediately restarted by the kubelet",
      "The Pod is deleted and a new one is scheduled",
      "The Node is cordoned and marked unschedulable"
    ],
    "answer": 0,
    "explanation": "A failing readiness probe only removes the Pod from Service endpoints (so it stops receiving traffic) — the container keeps running and is not restarted, unlike a failing liveness probe.",
    "reference": "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/",
    "tags": [
      "probes",
      "readiness"
    ]
  },
  {
    "id": "kubernetes-033",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of the following are valid mechanisms for defining a Pod probe (liveness, readiness, or startup)? (Select all that apply.)",
    "options": [
      "`httpGet`",
      "`tcpSocket`",
      "`exec`",
      "`grpc`",
      "`ping`"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "Kubernetes probes can be implemented as an HTTP GET request (`httpGet`), a TCP connection check (`tcpSocket`), an in-container command (`exec`), or a gRPC health check (`grpc`). There is no built-in `ping` probe mechanism.",
    "reference": "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/",
    "tags": [
      "probes"
    ]
  },
  {
    "id": "kubernetes-034",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "When the Kubernetes scheduler decides which node to place a Pod on, which value does it primarily use to check the node has enough capacity?",
    "options": [
      "The container's resource `requests`",
      "The container's resource `limits`",
      "The node's total memory only",
      "The Pod's `priorityClassName`"
    ],
    "answer": 0,
    "explanation": "The scheduler filters nodes based on whether they have enough allocatable capacity to satisfy the sum of a Pod's container `requests`; `limits` only bound runtime usage and are not used for the scheduling capacity check.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/",
    "tags": [
      "scheduling",
      "resources"
    ]
  },
  {
    "id": "kubernetes-035",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "A container exceeds its configured memory limit, and separately a container exceeds its configured CPU limit. What is the difference in outcome?",
    "options": [
      "Exceeding the memory limit gets the container OOMKilled, while exceeding the CPU limit only throttles it and does not kill it",
      "Both situations result in the container being immediately killed",
      "Exceeding the memory limit throttles the container, while exceeding the CPU limit kills it",
      "Neither has any effect unless a `LimitRange` is also defined"
    ],
    "answer": 0,
    "explanation": "Memory is not compressible: a container that exceeds its memory limit is terminated by the kernel's OOM killer. CPU is compressible: a container that exceeds its CPU limit is simply throttled (slowed down) via CFS quotas, not killed.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/",
    "tags": [
      "resources",
      "limits"
    ]
  },
  {
    "id": "kubernetes-036",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "multi",
    "prompt": "Which of these are valid Kubernetes Quality of Service (QoS) classes assigned to Pods? (Select all that apply.)",
    "options": [
      "Guaranteed",
      "Burstable",
      "BestEffort",
      "Reserved",
      "Priority"
    ],
    "answer": [
      0,
      1,
      2
    ],
    "explanation": "Kubernetes assigns every Pod one of three QoS classes based on its requests/limits: `Guaranteed` (requests equal limits for every container), `Burstable` (at least one request set, but not Guaranteed), or `BestEffort` (no requests or limits set at all). `Reserved` and `Priority` are not QoS classes.",
    "reference": "https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/",
    "tags": [
      "qos",
      "resources"
    ]
  },
  {
    "id": "kubernetes-037",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the simplest way to constrain a Pod to run only on nodes that have a specific label, such as `disktype=ssd`?",
    "options": [
      "Set `nodeSelector: { disktype: ssd }` in the Pod spec",
      "Set the Pod's `priorityClassName` to `ssd`",
      "Add a `taint` to the Pod",
      "Set the container's `resources.requests.disktype`"
    ],
    "answer": 0,
    "explanation": "`nodeSelector` is the simplest form of node selection constraint: the Pod is only scheduled onto nodes carrying all of the specified labels. Node affinity offers a more expressive alternative for complex rules.",
    "reference": "https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/",
    "tags": [
      "scheduling",
      "nodeselector"
    ]
  },
  {
    "id": "kubernetes-038",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "A Pod uses node affinity with `requiredDuringSchedulingIgnoredDuringExecution`. What does the `IgnoredDuringExecution` part mean?",
    "options": [
      "If a node's labels change after the Pod has been scheduled there, the Pod is not evicted even though it no longer matches the rule",
      "The rule is ignored entirely and has no effect on scheduling",
      "The Pod is evicted immediately if the node's labels ever change",
      "The rule only applies while the Pod is being deleted"
    ],
    "answer": 0,
    "explanation": "`IgnoredDuringExecution` means the affinity rule is only enforced at scheduling time; Kubernetes does not continuously re-evaluate it, so a running Pod stays on its node even if the node's labels later change and no longer satisfy the rule.",
    "reference": "https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/",
    "tags": [
      "scheduling",
      "affinity"
    ]
  },
  {
    "id": "kubernetes-039",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "multi",
    "prompt": "Which of the following are valid `effect` values for a node taint? (Select all that apply.)",
    "options": [
      "NoSchedule",
      "PreferNoSchedule",
      "NoExecute",
      "NoAdmit",
      "BlockSchedule"
    ],
    "answer": [
      0,
      1,
      2
    ],
    "explanation": "A taint's effect must be one of `NoSchedule` (don't schedule non-tolerating Pods), `PreferNoSchedule` (try to avoid it), or `NoExecute` (also evict already-running non-tolerating Pods). `NoAdmit` and `BlockSchedule` are not real taint effects.",
    "reference": "https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/",
    "tags": [
      "scheduling",
      "taints"
    ]
  },
  {
    "id": "kubernetes-040",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What does a `HorizontalPodAutoscaler` (HPA) do?",
    "options": [
      "Automatically adjusts the number of Pod replicas in a workload based on observed metrics such as CPU or memory usage",
      "Automatically increases a container's CPU and memory limits over time",
      "Automatically adds more nodes to the cluster",
      "Automatically resizes Persistent Volumes"
    ],
    "answer": 0,
    "explanation": "An HPA watches metrics (CPU/memory utilization, or custom/external metrics) and scales the number of replicas of a target Deployment, ReplicaSet, or StatefulSet up or down to meet a target value. Adding nodes is instead the job of a cluster autoscaler.",
    "reference": "https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/",
    "tags": [
      "hpa",
      "autoscaling"
    ]
  },
  {
    "id": "kubernetes-041",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "For a `HorizontalPodAutoscaler` to scale based on CPU or memory utilization, the metrics-server (or an equivalent metrics API) must be installed in the cluster.",
    "answer": true,
    "explanation": "The HPA controller reads resource metrics from the `metrics.k8s.io` API, which is served by metrics-server (or a compatible implementation); without it, CPU/memory-based autoscaling has no data to act on.",
    "reference": "https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/",
    "tags": [
      "hpa",
      "metrics-server"
    ]
  },
  {
    "id": "kubernetes-042",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "If a `Deployment`'s `spec.strategy.type` is left unset, which update strategy is used when the Pod template changes?",
    "options": [
      "RollingUpdate",
      "Recreate",
      "BlueGreen",
      "Canary"
    ],
    "answer": 0,
    "explanation": "`RollingUpdate` is the default Deployment strategy: old Pods are gradually replaced by new ones. `Recreate` must be explicitly set, and `BlueGreen`/`Canary` are not built-in Deployment strategies (they are patterns you implement yourself or via other tooling).",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/",
    "tags": [
      "deployment",
      "rollout"
    ]
  },
  {
    "id": "kubernetes-043",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "During a `RollingUpdate`, what do the `maxUnavailable` and `maxSurge` fields control?",
    "options": [
      "How many Pods can be unavailable, and how many extra Pods above the desired count can be created, during the rollout",
      "The maximum number of container restarts allowed per Pod",
      "The maximum time in seconds a rollout is allowed to run",
      "The maximum number of ReplicaSets kept in revision history"
    ],
    "answer": 0,
    "explanation": "`maxUnavailable` caps how many Pods can be down at once during the rollout, and `maxSurge` caps how many extra Pods beyond the desired replica count can be created temporarily, together controlling the rollout's speed versus availability trade-off.",
    "reference": "https://kubernetes.io/docs/concepts/workloads/controllers/deployment/",
    "tags": [
      "deployment",
      "rollout"
    ]
  },
  {
    "id": "kubernetes-044",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "fill",
    "prompt": "Write the `kubectl` command to stream the logs of a running Pod named `web-1`.",
    "answer": [
      "kubectl logs web-1"
    ],
    "explanation": "`kubectl logs <pod-name>` prints the logs of a Pod's (default) container; add `-f` to stream them continuously, or `-c <container>` to pick a specific container in a multi-container Pod.",
    "reference": "https://kubernetes.io/docs/reference/kubectl/cheatsheet/",
    "tags": [
      "kubectl",
      "logs"
    ],
    "pattern": "^kubectl\\s+logs\\s+web-1$"
  },
  {
    "id": "kubernetes-045",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "fill",
    "prompt": "Write the `kubectl` command to create or update resources defined in a manifest file named `app.yaml`.",
    "answer": [
      "kubectl apply -f app.yaml"
    ],
    "explanation": "`kubectl apply -f <file>` declaratively creates resources that don't yet exist and updates ones that do, reconciling the cluster state with what's defined in the manifest.",
    "reference": "https://kubernetes.io/docs/reference/kubectl/cheatsheet/",
    "tags": [
      "kubectl",
      "apply"
    ],
    "pattern": "^kubectl\\s+apply\\s+-f\\s+app\\.yaml$"
  },
  {
    "id": "kubernetes-046",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "How does `kubectl describe <resource> <name>` differ from `kubectl get <resource> <name>`?",
    "options": [
      "`describe` prints detailed, human-readable information including recent events, while `get` prints a concise summary (often a single table row)",
      "`describe` and `get` are exact aliases of each other",
      "`describe` deletes the resource after printing it, unlike `get`",
      "`get` shows recent events, while `describe` never does"
    ],
    "answer": 0,
    "explanation": "`kubectl get` is meant for quick, scriptable summaries (often one line per object), while `kubectl describe` gives a verbose, human-focused view of a single object, including its recent Events — very useful for debugging.",
    "reference": "https://kubernetes.io/docs/reference/kubectl/cheatsheet/",
    "tags": [
      "kubectl"
    ]
  },
  {
    "id": "kubernetes-047",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What does `kubectl rollout undo deployment/web` do?",
    "options": [
      "Reverts the `web` Deployment's Pods to the configuration used in the previous revision",
      "Deletes the `web` Deployment and all of its Pods permanently",
      "Pauses the current rollout without changing any existing Pods",
      "Scales the `web` Deployment down to zero replicas"
    ],
    "answer": 0,
    "explanation": "`kubectl rollout undo` rolls a Deployment back to its previous revision (or a specific one with `--to-revision`), using the revision history retained by the Deployment's ReplicaSets.",
    "reference": "https://kubernetes.io/docs/reference/kubectl/cheatsheet/",
    "tags": [
      "kubectl",
      "rollout"
    ]
  },
  {
    "id": "kubernetes-048",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "Which control-plane component exposes the Kubernetes REST API and is the central point of contact for all other components and clients, including `kubectl`?",
    "options": [
      "kube-apiserver",
      "etcd",
      "kube-scheduler",
      "kubelet"
    ],
    "answer": 0,
    "explanation": "kube-apiserver is the front end of the control plane; it validates and processes REST requests and is the only component that talks directly to etcd, with every other component and client communicating through it.",
    "reference": "https://kubernetes.io/docs/concepts/overview/components/",
    "tags": [
      "control-plane",
      "apiserver"
    ]
  },
  {
    "id": "kubernetes-049",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "`etcd` is the consistent, highly-available key-value store that holds all of a Kubernetes cluster's persistent state.",
    "answer": true,
    "explanation": "etcd is the cluster's single source of truth, storing the entire cluster state (objects, configuration, and status); only kube-apiserver talks to it directly.",
    "reference": "https://kubernetes.io/docs/concepts/overview/components/",
    "tags": [
      "control-plane",
      "etcd"
    ]
  },
  {
    "id": "kubernetes-050",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of the following are control-plane components of a Kubernetes cluster? (Select all that apply.)",
    "options": [
      "kube-apiserver",
      "etcd",
      "kube-scheduler",
      "kube-controller-manager",
      "kubelet",
      "kube-proxy"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "kube-apiserver, etcd, kube-scheduler, and kube-controller-manager (plus, in cloud environments, cloud-controller-manager) make up the control plane. `kubelet` and `kube-proxy` are node components that run on every worker (and typically control-plane) node instead.",
    "reference": "https://kubernetes.io/docs/concepts/overview/components/",
    "tags": [
      "control-plane"
    ]
  },
  {
    "id": "kubernetes-051",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of the following run as an agent on every worker node in a Kubernetes cluster? (Select all that apply.)",
    "options": [
      "kubelet",
      "kube-proxy",
      "A container runtime (e.g. containerd)",
      "kube-scheduler",
      "etcd (as the cluster's primary datastore)"
    ],
    "answer": [
      0,
      1,
      2
    ],
    "explanation": "Every node runs a `kubelet` (manages Pods on that node), `kube-proxy` (maintains network rules for Services), and a container runtime. `kube-scheduler` and the cluster's primary `etcd` datastore are control-plane components, not per-node agents.",
    "reference": "https://kubernetes.io/docs/concepts/overview/components/",
    "tags": [
      "node-components",
      "kubelet",
      "kube-proxy"
    ]
  },
  {
    "id": "kubernetes-052",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "In Kubernetes' networking model, how do Pods on different nodes communicate by default?",
    "options": [
      "Every Pod gets its own cluster-wide unique IP address, and Pods can reach each other directly without NAT",
      "Pods can only communicate through a Service; there is no direct Pod-to-Pod networking",
      "All Pods on a node share a single IP address and are distinguished by port",
      "Pod-to-Pod traffic is routed through the API server"
    ],
    "answer": 0,
    "explanation": "Kubernetes requires a 'flat' network model where every Pod gets its own unique IP address and can communicate with any other Pod's IP across nodes without NAT, which is what CNI plugins implement.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/",
    "tags": [
      "networking",
      "pod-ip"
    ]
  },
  {
    "id": "kubernetes-053",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "What role does a CNI (Container Network Interface) plugin play in a Kubernetes cluster?",
    "options": [
      "It implements the actual Pod networking (IP allocation and routing) so Pods on different nodes can communicate, fulfilling Kubernetes' networking model",
      "It builds and pushes container images to a registry",
      "It authenticates users against the API server",
      "It schedules Pods onto nodes based on resource requests"
    ],
    "answer": 0,
    "explanation": "Kubernetes itself does not implement Pod networking; it delegates that to a CNI plugin (for example Calico, Cilium, or Flannel), which is responsible for assigning Pod IPs and setting up routing so the required flat network model is realized.",
    "reference": "https://kubernetes.io/docs/concepts/cluster-administration/networking/",
    "tags": [
      "networking",
      "cni"
    ]
  },
  {
    "id": "kubernetes-054",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "CoreDNS is the default cluster add-on that provides DNS-based service discovery for Services and Pods in a Kubernetes cluster.",
    "answer": true,
    "explanation": "CoreDNS runs as Pods inside the cluster and serves DNS records (for example `my-svc.my-namespace.svc.cluster.local`) so that workloads can discover Services and Pods by name instead of hardcoding IPs. It replaced kube-dns as the default in modern Kubernetes.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/",
    "tags": [
      "networking",
      "dns",
      "coredns"
    ]
  },
  {
    "id": "kubernetes-055",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "If no `NetworkPolicy` objects exist in a namespace, Kubernetes' default behavior is to allow all pod-to-pod network traffic.",
    "answer": true,
    "explanation": "Kubernetes networking is non-isolating by default: traffic is unrestricted unless a `NetworkPolicy` selects the Pod, at which point that Pod's traffic becomes restricted to what the policy (or policies) allow. Enforcement additionally requires a CNI plugin that supports NetworkPolicy.",
    "reference": "https://kubernetes.io/docs/concepts/services-networking/network-policies/",
    "tags": [
      "networking",
      "networkpolicy"
    ]
  },
  {
    "id": "kubernetes-056",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is a Helm 'chart'?",
    "options": [
      "A package of pre-configured Kubernetes resource templates that can be deployed together as one unit",
      "A dashboard for visualizing cluster metrics",
      "A YAML file listing a cluster's nodes",
      "A built-in Kubernetes object for defining RBAC rules"
    ],
    "answer": 0,
    "explanation": "A Helm chart is a package format bundling Kubernetes manifest templates plus metadata and default configuration, letting you install, upgrade, and version an application as a single release.",
    "reference": "https://helm.sh/docs/topics/charts/",
    "tags": [
      "helm",
      "chart"
    ]
  },
  {
    "id": "kubernetes-057",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What is the purpose of a chart's `values.yaml` file in Helm?",
    "options": [
      "It supplies the default configuration values used to render the chart's templates, which can be overridden at install/upgrade time",
      "It lists the Kubernetes API versions supported by the cluster",
      "It stores the chart's release history",
      "It defines the RBAC Role bound to the chart's ServiceAccount"
    ],
    "answer": 0,
    "explanation": "`values.yaml` holds the default parameters referenced by a chart's templates (for example `{{ .Values.replicaCount }}`); users can override them with `--set` or a custom values file on `helm install`/`helm upgrade`.",
    "reference": "https://helm.sh/docs/chart_template_guide/values_files/",
    "tags": [
      "helm",
      "values"
    ]
  },
  {
    "id": "kubernetes-058",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "Helm 3 removed the server-side component called Tiller that Helm 2 required to be installed in the cluster.",
    "answer": true,
    "explanation": "Helm 2 needed a cluster-side component, Tiller, which raised security concerns due to its broad permissions. Helm 3 removed Tiller entirely, so the Helm client talks directly to the Kubernetes API server using the user's own credentials and RBAC.",
    "reference": "https://helm.sh/docs/intro/using_helm/",
    "tags": [
      "helm",
      "tiller"
    ]
  },
  {
    "id": "kubernetes-059",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the difference between a Custom Resource (CR) and a Custom Resource Definition (CRD)?",
    "options": [
      "A CRD defines a new API type (group, version, kind, schema); a CR is an instance of that type stored in the cluster",
      "A CR defines the schema; a CRD is one object created from it",
      "They are two names for the same object",
      "A CRD is only used by Helm, a CR is only used by operators"
    ],
    "answer": 0,
    "explanation": "The CRD registers a new resource type with the API server, including its OpenAPI schema and versions. Once the CRD exists, users create CRs of that kind just like built-in objects such as Deployments.",
    "reference": "https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/",
    "tags": [
      "crd",
      "operators"
    ]
  },
  {
    "id": "kubernetes-060",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "fill",
    "prompt": "When a Custom Resource is submitted through the Kubernetes API and accepted, which datastore does the API server persist it in?",
    "answer": [
      "etcd"
    ],
    "explanation": "All cluster state, including custom resources, is persisted by the kube-apiserver into etcd. Controllers never write to etcd directly; they always go through the API server.",
    "reference": "https://kubernetes.io/docs/concepts/architecture/#etcd",
    "tags": [
      "crd",
      "etcd",
      "architecture"
    ],
    "pattern": "^etcd$"
  },
  {
    "id": "kubernetes-061",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "After a Custom Resource is stored in etcd, what happens next?",
    "options": [
      "etcd executes the resource's logic directly",
      "The API server notifies clients watching that resource type; a controller or operator watching it receives the event and reconciles the desired state",
      "The kubelet on every node creates the resource locally",
      "Nothing happens until a user runs `kubectl rollout`"
    ],
    "answer": 1,
    "explanation": "Storing a CR by itself does nothing. The API server's watch mechanism delivers an ADDED/MODIFIED event to any client watching that kind. An operator's informer receives it, enqueues the object key, and the reconcile loop turns the desired state into real resources.",
    "reference": "https://kubernetes.io/docs/concepts/extend-kubernetes/operator/",
    "tags": [
      "crd",
      "operators",
      "watch"
    ]
  },
  {
    "id": "kubernetes-062",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of the following happen internally after you run `kubectl apply -f deploy.yaml`? (Select all that apply.)",
    "options": [
      "kubectl computes a patch by comparing the file, the live object, and the last-applied-configuration (or uses server-side apply with field managers)",
      "The API server authenticates and authorizes the request, then runs mutating and validating admission webhooks",
      "The API server validates the object against its schema and persists it in etcd",
      "The API server itself creates the Pods on the nodes",
      "Controllers (Deployment, ReplicaSet) watching the API react to the change and create or update child objects"
    ],
    "answer": [
      0,
      1,
      2,
      4
    ],
    "explanation": "The request flow is client-side diff or server-side apply, then authn/authz, admission, validation, and persistence in etcd. Pods are not created by the API server; the Deployment controller creates a ReplicaSet, the ReplicaSet controller creates Pods, the scheduler binds them to nodes, and the kubelet runs them.",
    "reference": "https://kubernetes.io/docs/reference/using-api/server-side-apply/",
    "tags": [
      "kubectl",
      "apiserver",
      "architecture"
    ]
  },
  {
    "id": "kubernetes-063",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "How does a Kubernetes controller or operator work at the most basic level?",
    "options": [
      "It runs a cron job that polls etcd every minute",
      "It watches objects via the API server, compares desired state (spec) with actual state, and takes actions to converge them",
      "It executes shell scripts embedded in the resource manifest",
      "It only reacts to `kubectl` commands, not to changes made by other clients"
    ],
    "answer": 1,
    "explanation": "A controller is a control loop: watch resources, observe the current state, compute the difference from the desired state, and act (create, update, delete) to close the gap. An operator is a controller that encodes operational knowledge for a specific application, usually driven by CRDs.",
    "reference": "https://kubernetes.io/docs/concepts/architecture/controller/",
    "tags": [
      "controllers",
      "operators"
    ]
  },
  {
    "id": "kubernetes-064",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What is a Kubernetes reconciliation loop?",
    "options": [
      "A loop that retries a failed `kubectl apply` until it succeeds",
      "A repeated process where a controller reads an object's desired state, observes the actual state, and performs actions to make them match, then re-runs on any change",
      "The API server's mechanism for compacting etcd history",
      "The kubelet's loop for restarting crashed containers"
    ],
    "answer": 1,
    "explanation": "Reconciliation is level-triggered rather than edge-triggered: the reconciler is given only an object key and must re-read current state and drive toward the spec regardless of which event woke it. This is why it tolerates missed or duplicated events.",
    "reference": "https://kubernetes.io/docs/concepts/architecture/controller/",
    "tags": [
      "controllers",
      "reconcile"
    ]
  },
  {
    "id": "kubernetes-065",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "single",
    "prompt": "What is the difference between `spec` and `status` in a Kubernetes object?",
    "options": [
      "`spec` is the desired state written by the user or client; `status` is the observed state written by the controller",
      "`spec` is read-only; `status` is what users edit to request changes",
      "`spec` is stored in etcd; `status` is only kept in memory",
      "They are interchangeable and both are written by the user"
    ],
    "answer": 0,
    "explanation": "The user declares intent in `spec`. Controllers report what they observed or achieved in `status`, typically via the `/status` subresource so that spec and status updates have separate RBAC and do not clobber each other.",
    "reference": "https://kubernetes.io/docs/concepts/overview/working-with-objects/#object-spec-and-status",
    "tags": [
      "spec",
      "status",
      "api"
    ]
  },
  {
    "id": "kubernetes-066",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "How does an operator typically detect that a Custom Resource has been created or updated?",
    "options": [
      "It polls `kubectl get` output on a fixed timer",
      "It receives a webhook call from etcd",
      "It uses an informer that opens a watch on the resource via the API server, keeps a local cache, and enqueues the object key into a work queue on events",
      "It tails the API server's audit log"
    ],
    "answer": 2,
    "explanation": "client-go informers do an initial list followed by a long-running watch. Events update a local indexed cache and trigger handlers that enqueue keys; worker goroutines pop keys and call reconcile. controller-runtime wraps this pattern.",
    "reference": "https://kubernetes.io/docs/reference/using-api/api-concepts/#efficient-detection-of-changes",
    "tags": [
      "operators",
      "informers",
      "watch"
    ]
  },
  {
    "id": "kubernetes-067",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "Why must a Kubernetes controller's reconcile function be idempotent?",
    "options": [
      "Because reconcile may be invoked many times for the same object (resyncs, duplicate events, retries after errors, restarts), and running it again must not create duplicate resources or cause side effects",
      "Because the API server rejects any request that is sent twice",
      "Because Go does not support retries",
      "Because etcd only stores each object once"
    ],
    "answer": 0,
    "explanation": "Reconcile is triggered by many sources and events can be coalesced or replayed. An idempotent reconcile checks current state and only acts on the difference, so repeated runs converge to the same result instead of, for example, creating a second Deployment.",
    "reference": "https://kubernetes.io/docs/concepts/architecture/controller/",
    "tags": [
      "controllers",
      "idempotency"
    ]
  },
  {
    "id": "kubernetes-068",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "What is a Kubernetes finalizer?",
    "options": [
      "A hook that runs after a Pod finishes so the kubelet can collect logs",
      "A key in `metadata.finalizers` that blocks actual deletion of an object until a controller performs cleanup and removes the key",
      "The last container in a Pod to be started",
      "A validating webhook that runs at the end of the admission chain"
    ],
    "answer": 1,
    "explanation": "When an object with finalizers is deleted, the API server sets `metadata.deletionTimestamp` instead of removing it. The controller sees this, cleans up external resources (cloud disks, DNS records), then removes its finalizer; once the list is empty the object is garbage collected.",
    "reference": "https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/",
    "tags": [
      "finalizers",
      "deletion"
    ]
  },
  {
    "id": "kubernetes-069",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "What is the difference between an `ownerReference` and a finalizer?",
    "options": [
      "An ownerReference lets the garbage collector delete dependent objects when their owner is deleted; a finalizer delays deletion of an object until a controller has done cleanup",
      "They are the same mechanism under different names",
      "A finalizer cascades deletion to children; an ownerReference blocks deletion",
      "ownerReferences apply only to Pods; finalizers apply only to CRs"
    ],
    "answer": 0,
    "explanation": "ownerReferences describe the parent/child graph used for cascading (background or foreground) deletion of in-cluster objects. Finalizers are for cleanup the garbage collector cannot do itself, such as deleting resources outside the cluster. Operators often use both: children carry an ownerReference to the CR, and the CR carries a finalizer for external cleanup.",
    "reference": "https://kubernetes.io/docs/concepts/overview/working-with-objects/owners-dependents/",
    "tags": [
      "finalizers",
      "ownerreferences",
      "garbage-collection"
    ]
  },
  {
    "id": "kubernetes-070",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "You are building a Kubernetes operator in Go. Which of the following are standard parts of that approach? (Select all that apply.)",
    "options": [
      "Scaffold the project with Kubebuilder or Operator SDK, which generate the CRD Go types and a controller-runtime manager",
      "Define the CR's `Spec` and `Status` structs with kubebuilder markers and run `controller-gen` to produce the CRD YAML and deepcopy functions",
      "Implement a `Reconcile(ctx, req)` method that fetches the object by name, creates or updates owned resources, updates status, and returns a `ctrl.Result`",
      "Write directly to etcd for performance",
      "Register the controller with `Owns()`/`Watches()` so changes to child resources also trigger reconcile"
    ],
    "answer": [
      0,
      1,
      2,
      4
    ],
    "explanation": "Kubebuilder and Operator SDK sit on controller-runtime, which provides the manager, cached client, informers, work queue and leader election. Reconcile receives only a namespaced name and must re-read state. Writing to etcd directly bypasses validation, admission and RBAC and is never done.",
    "reference": "https://book.kubebuilder.io/",
    "tags": [
      "operators",
      "golang",
      "kubebuilder",
      "controller-runtime"
    ]
  },
  {
    "id": "kubernetes-071",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "multi",
    "prompt": "Which RBAC permissions does a typical Kubernetes operator require? (Select all that apply.)",
    "options": [
      "get, list, watch on its own custom resource so it can cache and reconcile it",
      "update or patch on the custom resource's `/status` subresource to report observed state",
      "create, update, delete on the child resource kinds it manages, such as Deployments and Services",
      "cluster-admin, because operators always need full access",
      "create and patch on `events` so it can emit Kubernetes Events for troubleshooting",
      "update on the custom resource itself if it adds or removes finalizers"
    ],
    "answer": [
      0,
      1,
      2,
      4,
      5
    ],
    "explanation": "Follow least privilege: only the verbs and kinds the controller actually touches. Kubebuilder generates the ClusterRole from `+kubebuilder:rbac` markers. If leader election is enabled it also needs access to `leases` in coordination.k8s.io. cluster-admin is a common but poor shortcut.",
    "reference": "https://book.kubebuilder.io/reference/markers/rbac",
    "tags": [
      "operators",
      "rbac"
    ]
  },
  {
    "id": "kubernetes-072",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "In a controller-runtime operator, how should you handle a transient failure (for example an external API timeout) inside `Reconcile` so it is retried?",
    "options": [
      "Return the error; controller-runtime re-enqueues the request with rate-limited exponential backoff",
      "Call `os.Exit(1)` so the Pod restarts and reconcile runs again",
      "Sleep inside Reconcile until the external API recovers",
      "Ignore the error, since the next resync will fix it"
    ],
    "answer": 0,
    "explanation": "Returning an error triggers the work queue's rate limiter (exponential backoff, capped). For expected waits such as polling a slow resource, return `ctrl.Result{RequeueAfter: d}` with nil error. Blocking inside Reconcile starves other keys, and the default resync period is too coarse to rely on.",
    "reference": "https://book.kubebuilder.io/reference/reconcile",
    "tags": [
      "operators",
      "retries",
      "requeue"
    ]
  },
  {
    "id": "kubernetes-073",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "How would you manage different versions of a CRD (for example `v1alpha1` and `v1`) at the same time?",
    "options": [
      "Create two separate CRDs with different kinds",
      "List multiple versions in the CRD with exactly one marked as the storage version, and use a conversion webhook (or `None` strategy) so clients can read and write any served version",
      "Delete all CRs, change the version string, and recreate them",
      "Versions cannot coexist; only one version may ever be defined"
    ],
    "answer": 1,
    "explanation": "A CRD can serve several versions concurrently. Objects are persisted in the storage version; the API server converts on read/write using the conversion strategy. When the schemas differ, a conversion webhook implements the mapping. After migrating stored objects you can drop the old version from `served` and `storedVersions`.",
    "reference": "https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definition-versioning/",
    "tags": [
      "crd",
      "versioning",
      "conversion-webhook"
    ]
  },
  {
    "id": "kubernetes-074",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "A pod has been stuck in `Pending` for ten minutes. Which are legitimate steps or common causes to check? (Select all that apply.)",
    "options": [
      "Run `kubectl describe pod` and read the Events section for scheduler messages such as `Insufficient cpu` or `didn't match node affinity`",
      "Check whether the referenced PersistentVolumeClaim is bound, since a pod waiting on storage stays Pending",
      "Check node taints versus the pod's tolerations and any nodeSelector or affinity rules",
      "Check whether a ResourceQuota or LimitRange in the namespace is blocking the pod",
      "Restart the kubelet on every node, since Pending is always a kubelet problem"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "Pending means the scheduler has not bound the pod to a node (or the image pull has not started). The Events in `describe` almost always name the reason: insufficient resources, taints, affinity, unbound PVC, or quota. If the cluster is simply full, the Cluster Autoscaler or Karpenter logs show whether a node is being added. Restarting kubelets does not help a scheduling problem.",
    "reference": "https://kubernetes.io/docs/tasks/debug/debug-application/debug-pods/",
    "tags": [
      "troubleshooting",
      "scheduling",
      "pending"
    ]
  },
  {
    "id": "kubernetes-075",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "multi",
    "prompt": "A pod is `Running` but clients cannot reach it through its Service. Which checks are appropriate? (Select all that apply.)",
    "options": [
      "Run `kubectl get endpoints <svc>` (or EndpointSlices) to confirm the pod IP is listed; if empty, the Service selector does not match the pod labels or the readiness probe is failing",
      "Verify the Service `port` and `targetPort` match the port the container actually listens on",
      "Exec into another pod and `curl` the pod IP directly to separate an app problem from a Service or DNS problem",
      "Check NetworkPolicies that may be denying ingress to the pod",
      "Delete the Service and recreate it with a NodePort, since ClusterIP Services are unreliable"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "Work from the inside out: is the app listening on the expected port, does the pod pass readiness so it is in the Endpoints, does the Service map the right ports, does DNS resolve the Service name, and is a NetworkPolicy blocking traffic. Changing Service type does not address any of these.",
    "reference": "https://kubernetes.io/docs/tasks/debug/debug-application/debug-service/",
    "tags": [
      "troubleshooting",
      "service",
      "networking"
    ]
  },
  {
    "id": "kubernetes-076",
    "domain": "kubernetes",
    "difficulty": "easy",
    "type": "fill",
    "prompt": "Before performing maintenance on a worker node you run `kubectl cordon <node>` and then which `kubectl` subcommand to evict the pods gracefully, respecting PodDisruptionBudgets?",
    "answer": [
      "drain",
      "kubectl drain"
    ],
    "explanation": "`kubectl drain` marks the node unschedulable and evicts pods using the Eviction API, which honours PodDisruptionBudgets. You typically add `--ignore-daemonsets` and `--delete-emptydir-data`. After maintenance, `kubectl uncordon` makes the node schedulable again.",
    "reference": "https://kubernetes.io/docs/tasks/administer-cluster/safely-drain-node/",
    "tags": [
      "nodes",
      "maintenance",
      "drain"
    ],
    "pattern": "^(kubectl\\s+)?drain$"
  },
  {
    "id": "kubernetes-077",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "Which sequence safely performs maintenance on a Kubernetes worker node with minimal disruption?",
    "options": [
      "Power off the node, wait for pods to be rescheduled, power it back on",
      "Ensure PodDisruptionBudgets exist for critical workloads, `kubectl cordon` the node, `kubectl drain --ignore-daemonsets` it, do the maintenance, then `kubectl uncordon`",
      "Delete all pods on the node with `--force --grace-period=0`, then reboot",
      "Scale every Deployment to zero, reboot, then scale back up"
    ],
    "answer": 1,
    "explanation": "Cordon stops new pods landing on the node, drain evicts existing ones gracefully while PDBs prevent taking too many replicas of a service down at once, and uncordon returns the node to service. Force-deleting pods or powering off bypasses graceful termination and PDBs.",
    "reference": "https://kubernetes.io/docs/tasks/administer-cluster/safely-drain-node/",
    "tags": [
      "nodes",
      "maintenance",
      "pdb"
    ]
  },
  {
    "id": "kubernetes-078",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "How does persistent storage work in Kubernetes?",
    "options": [
      "Containers write to their own filesystem, which is automatically preserved across restarts",
      "A pod references a PersistentVolumeClaim; a StorageClass and CSI driver dynamically provision a PersistentVolume that is bound to the claim and mounted into the pod",
      "The kubelet copies data between nodes whenever a pod is rescheduled",
      "Only StatefulSets can use storage; Deployments are always stateless"
    ],
    "answer": 1,
    "explanation": "PVCs are the pod's request for storage; PVs are the actual volumes. With dynamic provisioning the StorageClass names a CSI driver (for example EBS on EKS) that creates the backing disk. Access modes (RWO, RWX) and the volume binding mode (for example `WaitForFirstConsumer`) determine which nodes can mount it.",
    "reference": "https://kubernetes.io/docs/concepts/storage/persistent-volumes/",
    "tags": [
      "storage",
      "pv",
      "pvc",
      "csi"
    ]
  },
  {
    "id": "kubernetes-079",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "How is access to an Amazon EKS cluster typically managed for human users and CI pipelines?",
    "options": [
      "Every user shares the cluster creator's kubeconfig",
      "IAM principals are mapped to Kubernetes users/groups (via EKS access entries or the legacy `aws-auth` ConfigMap), and Kubernetes RBAC Roles/ClusterRoles bound to those groups control what they can do",
      "EKS ignores RBAC; permissions come only from IAM policies",
      "Users log in with a username and password stored in a Secret"
    ],
    "answer": 1,
    "explanation": "Authentication is IAM: `aws eks get-token` produces a token the API server verifies. Authorization is two layers: the IAM-to-Kubernetes identity mapping (access entries with access policies, or aws-auth), then normal RBAC inside the cluster. Workloads use IRSA or Pod Identity to get IAM permissions.",
    "reference": "https://docs.aws.amazon.com/eks/latest/userguide/access-entries.html",
    "tags": [
      "eks",
      "rbac",
      "iam"
    ]
  },
  {
    "id": "kubernetes-080",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "multi",
    "prompt": "Which practices help upgrade a Kubernetes cluster without disrupting critical services? (Select all that apply.)",
    "options": [
      "Upgrade the control plane first, then node groups, staying within the supported version skew for kubelet and kubectl",
      "Check for removed or deprecated APIs in the target version (for example with `kubectl-convert` or Pluto) and update manifests, Helm charts and operators beforehand",
      "Define PodDisruptionBudgets and run at least two replicas spread across nodes so draining does not take a service fully down",
      "Use rolling or surge node-group upgrades that add new nodes before draining old ones, and upgrade cluster add-ons (CNI, CoreDNS, kube-proxy) to compatible versions",
      "Upgrade several minor versions in one jump to reduce the number of maintenance windows"
    ],
    "answer": [
      0,
      1,
      2,
      3
    ],
    "explanation": "Kubernetes only supports upgrading one minor version at a time, and the API server must be newer than or equal to kubelets. Test in a non-production cluster first, watch for API removals, and rely on PDBs and surge capacity so drains are graceful. Skipping minor versions is unsupported.",
    "reference": "https://kubernetes.io/releases/version-skew-policy/",
    "tags": [
      "upgrades",
      "version-skew",
      "pdb"
    ]
  },
  {
    "id": "kubernetes-081",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "multi",
    "prompt": "You want a CRD, its operator and its Helm chart to be portable across different Kubernetes clusters (different cloud providers, versions and namespaces). Which practices help? (Select all that apply.)",
    "options": [
      "Avoid hard-coding cluster-specific values (StorageClass names, ingress classes, domains, IAM roles) and expose them as Helm values",
      "Ship the CRDs with the chart (`crds/` directory or a separate chart) and pin the operator to stable served API versions of built-in resources",
      "Make the operator namespace-agnostic by reading its namespace from the Downward API and using `WATCH_NAMESPACE` or cluster scope as configured",
      "Compile the operator binary for a single architecture only, since all clusters run amd64",
      "Set an explicit `kubeVersion` constraint in `Chart.yaml` and test the chart against each supported version"
    ],
    "answer": [
      0,
      1,
      2,
      4
    ],
    "explanation": "Portability means parameterising anything that differs by cluster, avoiding dependence on provider-specific defaults, building multi-arch images (arm64 nodes are common), and declaring which Kubernetes versions the chart and operator support. Helm's `crds/` directory installs CRDs before templates are rendered but does not upgrade them, so document or automate CRD upgrades.",
    "reference": "https://helm.sh/docs/chart_best_practices/custom_resource_definitions/",
    "tags": [
      "helm",
      "crd",
      "operators",
      "portability"
    ]
  },
  {
    "id": "kubernetes-082",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "Deleting a CustomResourceDefinition also deletes every Custom Resource of that kind in the cluster.",
    "answer": true,
    "explanation": "Removing a CRD removes the API type, and the API server garbage-collects all objects of that kind. If those CRs carry finalizers whose controller is already gone, the CRD deletion hangs until the finalizers are removed manually.",
    "reference": "https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/#delete-a-customresourcedefinition",
    "tags": [
      "crd",
      "deletion"
    ]
  },
  {
    "id": "kubernetes-083",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "When the `status` subresource is enabled on a CRD, an update to the main resource endpoint ignores changes to `.status`, and an update to `/status` ignores changes to `.spec`.",
    "answer": true,
    "explanation": "Enabling `subresources.status` splits the object: PUT/PATCH on the resource only persists spec and metadata, while PUT/PATCH on `/status` only persists status. This prevents controllers and users from overwriting each other and allows separate RBAC rules for each.",
    "reference": "https://kubernetes.io/docs/tasks/extend-kubernetes/custom-resources/custom-resource-definitions/#status-subresource",
    "tags": [
      "crd",
      "status",
      "subresources"
    ]
  },
  {
    "id": "kubernetes-084",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "What is the purpose of `status.observedGeneration` in a controller-managed resource?",
    "options": [
      "It counts how many times the object has been reconciled",
      "It records the `metadata.generation` the controller last acted on, so clients can tell whether the reported status corresponds to the current spec",
      "It is the etcd revision number of the object",
      "It is the number of child objects the controller owns"
    ],
    "answer": 1,
    "explanation": "`metadata.generation` increments on every spec change. A controller copies it into `status.observedGeneration` after reconciling. If they differ, the status is stale and the controller has not yet processed the latest spec, which is how tools like `kubectl rollout status` decide whether to keep waiting.",
    "reference": "https://kubernetes.io/docs/reference/kubernetes-api/workload-resources/deployment-v1/#DeploymentStatus",
    "tags": [
      "status",
      "generation",
      "controllers"
    ]
  },
  {
    "id": "kubernetes-085",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "Several pods on one EKS node are repeatedly evicted and the node reports the `DiskPressure` condition. Which is the most likely root cause and fix?",
    "options": [
      "The node's root volume is full of unused container images or overflowing container logs; prune images, enforce log rotation and set ephemeral-storage requests/limits, and consider a larger root volume",
      "The API server has run out of etcd space; compact and defragment etcd",
      "The pods are missing a livenessProbe; add one",
      "The Cluster Autoscaler has scaled down too aggressively; disable it"
    ],
    "answer": 0,
    "explanation": "DiskPressure means the kubelet's eviction thresholds for `nodefs` or `imagefs` were crossed. Typical culprits are image bloat, application logs written to the node, or emptyDir volumes. `kubectl describe node` and `df` on the node confirm it; ephemeral-storage limits let the kubelet evict the offending pod rather than random ones.",
    "reference": "https://kubernetes.io/docs/concepts/scheduling-eviction/node-pressure-eviction/",
    "tags": [
      "troubleshooting",
      "eviction",
      "nodes",
      "incident"
    ]
  },
  {
    "id": "kubernetes-086",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "A Deployment's pods keep restarting with `OOMKilled`. Which action addresses the cause rather than the symptom?",
    "options": [
      "Remove the livenessProbe so Kubernetes stops restarting the container",
      "Inspect memory usage (`kubectl top pod`, APM heap metrics) and either fix the leak or raise the container's memory limit to fit its real working set",
      "Increase the number of replicas so each pod gets less traffic",
      "Set `restartPolicy: Never`"
    ],
    "answer": 1,
    "explanation": "OOMKilled means the container exceeded its cgroup memory limit and the kernel killed it. The fix is right-sizing the limit or fixing the application's memory growth. Removing probes or changing restart policy just hides the failure; more replicas only helps if memory scales with traffic.",
    "reference": "https://kubernetes.io/docs/tasks/configure-pod-container/assign-memory-resource/",
    "tags": [
      "troubleshooting",
      "oom",
      "resources"
    ]
  },
  {
    "id": "kubernetes-087",
    "domain": "kubernetes",
    "difficulty": "medium",
    "type": "single",
    "prompt": "Which statement correctly distinguishes the Horizontal Pod Autoscaler (HPA) from the Cluster Autoscaler or Karpenter?",
    "options": [
      "HPA changes the number of pod replicas based on metrics such as CPU or custom metrics; the Cluster Autoscaler/Karpenter adds or removes nodes when pods cannot be scheduled or nodes are underutilised",
      "HPA adds nodes; the Cluster Autoscaler adds pods",
      "They are the same component configured differently",
      "HPA only works with StatefulSets"
    ],
    "answer": 0,
    "explanation": "Autoscaling in Kubernetes has two layers. HPA (and KEDA for event-driven sources like SQS queue depth) scales workloads. When new pods go Pending for lack of capacity, the node autoscaler provisions nodes; when nodes are mostly empty it consolidates them. VPA is a third option that adjusts requests instead of replica count.",
    "reference": "https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/",
    "tags": [
      "autoscaling",
      "hpa",
      "cluster-autoscaler"
    ]
  },
  {
    "id": "kubernetes-088",
    "domain": "kubernetes",
    "difficulty": "hard",
    "type": "single",
    "prompt": "You run `kubectl delete` on a custom resource and it stays in `Terminating` indefinitely. What is the most likely reason?",
    "options": [
      "The API server is waiting for etcd compaction",
      "The object has a finalizer and the controller responsible for removing it is not running or is failing",
      "The pod's grace period is set too high",
      "The CRD has multiple versions defined"
    ],
    "answer": 1,
    "explanation": "An object with `deletionTimestamp` set and a non-empty `metadata.finalizers` list is not removed until each finalizer is cleared. If the operator that owns the finalizer is down, the object hangs. Restore the operator, or as a last resort patch the finalizers away after doing the cleanup manually.",
    "reference": "https://kubernetes.io/docs/concepts/overview/working-with-objects/finalizers/",
    "tags": [
      "finalizers",
      "troubleshooting",
      "deletion"
    ]
  }
]
