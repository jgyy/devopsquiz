[
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "Cilium replaces traditional iptables-based packet processing. Which kernel technology does the Cilium datapath use to implement networking, load balancing and policy enforcement?",
    "options": [
      "eBPF programs attached to kernel hooks such as tc and XDP",
      "Netfilter chains generated per Service by cilium-agent",
      "A user-space Envoy proxy that handles every packet",
      "Open vSwitch flow tables managed by the operator"
    ],
    "optionNotes": [
      "Correct. cilium-agent compiles and loads eBPF programs into the kernel at hook points like tc ingress/egress and XDP, so forwarding, service load balancing and policy decisions happen in-kernel without iptables rules.",
      "Cilium's goal is to avoid the O(n) iptables/netfilter chains of kube-proxy; it does not generate netfilter chains per Service.",
      "Envoy is only used for L7 policy and ingress traffic that is explicitly redirected; ordinary L3/L4 traffic never leaves the kernel.",
      "Cilium does not use Open vSwitch; flow state lives in eBPF maps."
    ],
    "answer": 0,
    "explanation": "Cilium is built on eBPF: the agent generates eBPF bytecode and attaches it to kernel hooks (tc, XDP, socket hooks) on each node, and the programs consult eBPF maps for identities, policies and service backends. This avoids the linear iptables chains kube-proxy relies on; Envoy is only involved for L7-aware features.",
    "example": "# inspect loaded eBPF programs and maps on a node\nkubectl -n kube-system exec ds/cilium -- cilium-dbg bpf lb list\nkubectl -n kube-system exec ds/cilium -- cilium-dbg bpf policy get --all",
    "diagram": "flowchart LR\n    A[\"cilium-agent\"] -->|\"compiles + loads\"| P[\"eBPF programs\"]\n    P -->|\"attached at\"| H[\"tc / XDP / socket hooks\"]\n    P -->|\"lookups\"| M[\"eBPF maps: identities, policy, services\"]\n    Pod[\"Pod traffic\"] --> H\n    H -->|\"in-kernel decision\"| Out[\"forward / drop / redirect to Envoy\"]",
    "reference": "https://docs.cilium.io/en/stable/overview/component-overview/",
    "tags": [
      "cca",
      "architecture",
      "ebpf"
    ],
    "id": "cilium-001",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "In a Cilium installation using the default `cluster-pool` IPAM mode, which component is responsible for allocating a PodCIDR to each node?",
    "options": [
      "cilium-operator",
      "cilium-agent on that node",
      "The Hubble Relay deployment",
      "kube-proxy"
    ],
    "optionNotes": [
      "Correct. The operator is a cluster-wide Deployment that handles tasks that only need to happen once per cluster, including carving per-node PodCIDRs out of the cluster pool and writing them into the `CiliumNode` resource.",
      "The agent (a DaemonSet) consumes the PodCIDR assigned to its node and hands out individual pod IPs, but it does not allocate node ranges in cluster-pool mode.",
      "Hubble Relay only aggregates observability flows from all agents; it has nothing to do with IPAM.",
      "kube-proxy is the component Cilium typically replaces; it never manages pod addressing."
    ],
    "answer": 0,
    "explanation": "cilium-operator is responsible for cluster-scoped duties: cluster-pool IPAM allocation of PodCIDRs to nodes, garbage collection of stale `CiliumIdentity` and `CiliumEndpoint` objects, and synchronising Kubernetes Services to the kvstore when one is used. cilium-agent runs on every node, loads the eBPF datapath, enforces policy and allocates pod IPs from the node's assigned range.",
    "example": "kubectl get ciliumnodes -o custom-columns=NAME:.metadata.name,PODCIDR:.spec.ipam.podCIDRs\n# NAME     PODCIDR\n# node-a   [10.0.0.0/24]\n# node-b   [10.0.1.0/24]   # ranges carved out by cilium-operator",
    "diagram": "flowchart TD\n    OP[\"cilium-operator Deployment\"] -->|\"allocates PodCIDR per node\"| CN[\"CiliumNode CR\"]\n    OP -->|\"GC identities + endpoints\"| ID[\"CiliumIdentity / CiliumEndpoint\"]\n    CN --> AG[\"cilium-agent DaemonSet\"]\n    AG -->|\"assigns pod IPs\"| POD[\"Pods on node\"]\n    AG -->|\"loads eBPF, enforces policy\"| K[\"Kernel datapath\"]",
    "reference": "https://docs.cilium.io/en/stable/network/concepts/ipam/cluster-pool/",
    "tags": [
      "cca",
      "architecture",
      "operator"
    ],
    "id": "cilium-002",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "When Cilium is installed with `kubeProxyReplacement=true`, the eBPF datapath implements ClusterIP, NodePort and LoadBalancer Service handling, so the `kube-proxy` DaemonSet can be removed from the cluster entirely.",
    "answer": true,
    "explanation": "Cilium's kube-proxy replacement uses eBPF (socket-level load balancing for ClusterIP and tc/XDP programs for NodePort/LoadBalancer) to implement Kubernetes Services, which makes kube-proxy redundant. Clusters are commonly bootstrapped with `--skip-phases=addon/kube-proxy` (kubeadm) and the API server address passed via `k8sServiceHost`/`k8sServicePort` because the agent can no longer rely on kube-proxy to reach the `kubernetes` Service.",
    "example": "cilium install --version 1.17.0 \\\n  --set kubeProxyReplacement=true \\\n  --set k8sServiceHost=10.0.0.10 \\\n  --set k8sServicePort=6443\nkubectl -n kube-system exec ds/cilium -- cilium-dbg status | grep KubeProxyReplacement\n# KubeProxyReplacement:    True   [eth0 ...]",
    "diagram": "flowchart LR\n    S[\"Kubernetes Service\"] --> A[\"cilium-agent watches Services\"]\n    A --> M[\"eBPF LB maps\"]\n    C[\"Client Pod connect\"] -->|\"socket hook\"| M\n    M -->|\"picks backend\"| B[\"Backend Pod\"]\n    KP[\"kube-proxy iptables\"] -.->|\"no longer needed\"| S",
    "reference": "https://docs.cilium.io/en/stable/network/kubernetes/kubeproxy-free/",
    "tags": [
      "cca",
      "kube-proxy-replacement",
      "services"
    ],
    "id": "cilium-003",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "Two Pods carry the same label `app=web`, but one runs in namespace `dev` and the other in `prod`. Which statement about their Cilium security identities is correct?",
    "options": [
      "They get different identities because the namespace is part of the security-relevant labels",
      "They share one identity because only user-defined labels are considered",
      "They get different identities because identities are derived from Pod IP addresses",
      "They share one identity because both are managed by the same cilium-operator"
    ],
    "optionNotes": [
      "Correct. Cilium adds `k8s:io.kubernetes.pod.namespace=<ns>` (and other `k8s:` labels) to every endpoint, so the label sets differ and a distinct numeric identity is allocated for each.",
      "The namespace is always included in the identity-relevant label set precisely so that identically-labelled workloads in different namespaces cannot impersonate each other.",
      "Identities are label-based, not IP-based; that is what allows policy to survive pod churn and IP reuse.",
      "The operator only garbage-collects identities; it does not decide which endpoints share one."
    ],
    "answer": 0,
    "explanation": "A Cilium identity is a numeric ID derived from an endpoint's set of security-relevant labels, which always includes the Kubernetes namespace label. Endpoints with an identical label set share the identity, and policy is enforced by comparing identities carried in packets rather than IP addresses.",
    "example": "kubectl -n kube-system exec ds/cilium -- cilium-dbg endpoint list\n# ENDPOINT  POLICY (ingress) IDENTITY  LABELS (source:key[=value])\n# 1234      Enabled          28417     k8s:app=web k8s:io.kubernetes.pod.namespace=dev\n# 2345      Enabled          31905     k8s:app=web k8s:io.kubernetes.pod.namespace=prod\nkubectl get ciliumidentities",
    "diagram": "flowchart LR\n    P1[\"Pod app=web ns=dev\"] --> L1[\"labels + k8s namespace\"]\n    P2[\"Pod app=web ns=prod\"] --> L2[\"labels + k8s namespace\"]\n    L1 -->|\"hash of label set\"| I1[\"Identity 28417\"]\n    L2 -->|\"hash of label set\"| I2[\"Identity 31905\"]\n    I1 --> CI[\"CiliumIdentity CRs\"]\n    I2 --> CI",
    "reference": "https://docs.cilium.io/en/stable/security/network/identity/",
    "tags": [
      "cca",
      "identity",
      "endpoints"
    ],
    "id": "cilium-004",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "fill",
    "prompt": "After installing Cilium with the `cilium` CLI, which single CLI subcommand prints a summary of the agent, operator and Hubble health, including the number of managed endpoints and any errors or warnings? Give the full command (two words).",
    "answer": [
      "cilium status",
      "cilium status --wait"
    ],
    "pattern": "^cilium\\s+status(\\s+--wait)?$",
    "explanation": "`cilium status` queries the DaemonSet, operator and Hubble deployments and prints an overview with per-component OK/Error lines, image versions and warnings. `--wait` blocks until everything is ready, which is convenient right after `cilium install`. `cilium-dbg status` inside an agent pod gives the per-node view.",
    "example": "cilium status --wait\n#     /¯¯\\\n#  /¯¯\\__/¯¯\\    Cilium:             OK\n#  \\__/¯¯\\__/    Operator:           OK\n#  /¯¯\\__/¯¯\\    Hubble Relay:       OK\n#  \\__/¯¯\\__/    ClusterMesh:        disabled\n# DaemonSet  cilium  Desired: 3, Ready: 3/3",
    "diagram": "flowchart LR\n    C[\"cilium status\"] --> A[\"cilium DaemonSet ready?\"]\n    C --> O[\"cilium-operator ready?\"]\n    C --> H[\"hubble-relay / hubble-ui ready?\"]\n    C --> E[\"errors and warnings per pod\"]\n    A --> R[\"summary table\"]\n    O --> R\n    H --> R",
    "reference": "https://docs.cilium.io/en/stable/gettingstarted/k8s-install-default/",
    "tags": [
      "cca",
      "installation",
      "cli"
    ],
    "id": "cilium-005",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "Right after installing Cilium you want an end-to-end check that pod-to-pod, pod-to-service, and pod-to-external traffic work, including policy enforcement, by deploying temporary test workloads. Which command does this?",
    "options": [
      "`cilium connectivity test`",
      "`cilium status --wait`",
      "`cilium sysdump`",
      "`hubble observe --follow`"
    ],
    "optionNotes": [
      "Correct. `cilium connectivity test` creates a `cilium-test` namespace with client and echo deployments and runs dozens of scenarios (pod-to-pod, to service, to world, with and without network policies), then reports pass/fail.",
      "`cilium status` only checks whether the components are running; it sends no traffic.",
      "`cilium sysdump` collects logs and state into an archive for troubleshooting or bug reports; it does not test connectivity.",
      "`hubble observe` shows flows that already exist; it does not generate test traffic."
    ],
    "answer": 0,
    "explanation": "`cilium connectivity test` is the canonical smoke test after installation: it deploys test workloads and runs a suite of scenarios that exercise the datapath, DNS, network policy and optionally Cluster Mesh, then deletes them. `cilium status` and `cilium sysdump` are health and diagnostic tools and do not generate traffic.",
    "example": "cilium connectivity test\n# ℹ️  Deploying echo-same-node service...\n# [=] Test [no-policies]\n# [=] Test [client-egress-l7]\n# ✅ All 46 tests (326 actions) successful, 12 tests skipped\ncilium connectivity test --test 'to-fqdns'   # run a subset",
    "diagram": "flowchart TD\n    T[\"cilium connectivity test\"] --> NS[\"creates cilium-test namespace\"]\n    NS --> W[\"client + echo deployments\"]\n    W --> S1[\"pod-to-pod\"]\n    W --> S2[\"pod-to-service\"]\n    W --> S3[\"pod-to-world\"]\n    W --> S4[\"policy scenarios\"]\n    S1 --> R[\"pass / fail report\"]\n    S4 --> R",
    "reference": "https://docs.cilium.io/en/stable/gettingstarted/k8s-install-default/#validate-the-installation",
    "tags": [
      "cca",
      "installation",
      "connectivity"
    ],
    "id": "cilium-006",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "A team must allow only `GET /healthz` and `GET /api/*` from the frontend to the backend while denying every other HTTP request. Which policy resource is required to express this?",
    "options": [
      "A `CiliumNetworkPolicy` with an L7 `http` rule",
      "A standard Kubernetes `NetworkPolicy` with a port rule",
      "An `Ingress` object with path-based routing",
      "A `CiliumNode` annotation"
    ],
    "optionNotes": [
      "Correct. Only Cilium's own policy CRDs (`CiliumNetworkPolicy` and `CiliumClusterwideNetworkPolicy`) support L7 rules such as HTTP method and path, DNS names, or Kafka topics; matching traffic is redirected to the embedded Envoy proxy.",
      "Kubernetes `NetworkPolicy` is limited to L3/L4 (pod/namespace selectors, IP blocks and ports); it cannot see HTTP methods or paths.",
      "An Ingress routes north-south traffic into the cluster; it does not restrict pod-to-pod HTTP calls.",
      "`CiliumNode` describes node state (addresses, PodCIDRs); it is not a policy resource."
    ],
    "answer": 0,
    "explanation": "Cilium fully enforces standard `NetworkPolicy` at L3/L4, but `CiliumNetworkPolicy` extends it with identity/entity-based rules, `toFQDNs`, and L7 rules for HTTP, DNS and Kafka. When an L7 rule is present, matching flows are transparently proxied through Envoy where the method and path are evaluated.",
    "example": "apiVersion: cilium.io/v2\nkind: CiliumNetworkPolicy\nmetadata:\n  name: backend-l7\nspec:\n  endpointSelector: {matchLabels: {app: backend}}\n  ingress:\n  - fromEndpoints:\n    - matchLabels: {app: frontend}\n    toPorts:\n    - ports: [{port: \"8080\", protocol: TCP}]\n      rules:\n        http:\n        - {method: GET, path: \"/healthz\"}\n        - {method: GET, path: \"/api/.*\"}",
    "diagram": "flowchart LR\n    F[\"frontend Pod\"] --> E[\"eBPF: L3/L4 allow, L7 rule present\"]\n    E -->|\"redirect\"| P[\"Envoy proxy in agent\"]\n    P -->|\"GET /api/x allowed\"| B[\"backend Pod\"]\n    P -->|\"POST /admin 403\"| D[\"denied\"]\n    K[\"K8s NetworkPolicy\"] -.->|\"L3/L4 only\"| E",
    "reference": "https://docs.cilium.io/en/stable/security/policy/language/#layer-7-examples",
    "tags": [
      "cca",
      "network-policy",
      "l7"
    ],
    "id": "cilium-007",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "A `CiliumNetworkPolicy` has an egress rule `toFQDNs: [{matchName: api.github.com}]` on port 443, yet the selected pod still cannot reach the API and `hubble observe` shows DNS lookups being dropped. What is missing from the policy?",
    "options": [
      "An egress rule allowing UDP/TCP 53 to kube-dns with an L7 `dns` rule so the Cilium DNS proxy can observe the lookups",
      "A `toCIDR` rule for the current IP of `api.github.com`",
      "The `enable-l7-proxy` flag must be turned off because FQDN policies bypass Envoy",
      "A `toEntities: [world]` rule, because FQDN rules only work together with the world entity"
    ],
    "optionNotes": [
      "Correct. `toFQDNs` works by having the agent's DNS proxy intercept DNS responses and populate the allowed IP set; once the policy makes the endpoint default-deny for egress, DNS itself must be explicitly allowed and routed through the proxy via a `rules: dns` block.",
      "Hardcoding IPs defeats the purpose of FQDN policy and would break as soon as the address rotates; it is not the intended fix.",
      "The DNS proxy is part of the agent and is required for FQDN policy; disabling L7 proxying would break it further.",
      "Adding the `world` entity would allow all external traffic, which is far broader than intended and is not a prerequisite of `toFQDNs`."
    ],
    "answer": 0,
    "explanation": "When any egress rule selects an endpoint, all other egress is denied, including DNS. The canonical pattern is a second egress rule that allows port 53 to the kube-dns pods with `rules: dns: [{matchPattern: \"*\"}]`, which redirects DNS through Cilium's DNS proxy; the proxy records the resolved IPs and programs them into the `toFQDNs` allow set.",
    "example": "egress:\n- toEndpoints:\n  - matchLabels: {k8s:io.kubernetes.pod.namespace: kube-system, k8s-app: kube-dns}\n  toPorts:\n  - ports: [{port: \"53\", protocol: ANY}]\n    rules:\n      dns:\n      - matchPattern: \"*\"      # lets the DNS proxy learn resolved IPs\n- toFQDNs:\n  - matchName: api.github.com\n  toPorts:\n  - ports: [{port: \"443\", protocol: TCP}]",
    "diagram": "sequenceDiagram\n    participant P as Pod\n    participant D as Cilium DNS proxy\n    participant K as kube-dns\n    participant E as eBPF policy map\n    P->>D: DNS query api.github.com\n    D->>K: forward query\n    K-->>D: A 140.82.x.x\n    D->>E: add IP to toFQDNs allow set\n    D-->>P: response\n    P->>E: TCP 443 to 140.82.x.x allowed",
    "reference": "https://docs.cilium.io/en/stable/security/policy/language/#dns-based",
    "tags": [
      "cca",
      "network-policy",
      "fqdn"
    ],
    "id": "cilium-008",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which of these are valid built-in entity names usable in a `CiliumNetworkPolicy` `toEntities` or `fromEntities` list? (Select all that apply.)",
    "options": [
      "`world`",
      "`host`",
      "`kube-apiserver`",
      "`internet`",
      "`namespace`"
    ],
    "optionNotes": [
      "`world` matches all endpoints outside the cluster, i.e. any destination not managed by Cilium; it is the standard way to allow egress to the internet.",
      "`host` matches the local node itself (including host-networked pods on that node).",
      "`kube-apiserver` matches the Kubernetes API server, letting you allow control-plane access without knowing its IP.",
      "`internet` is not a Cilium entity; the equivalent concept is `world`.",
      "`namespace` is not an entity; namespace scoping is done with `matchLabels` on `k8s:io.kubernetes.pod.namespace` or by the policy's own namespace."
    ],
    "answer": [
      0,
      1,
      2
    ],
    "explanation": "Cilium entities are predefined identity groups: `all`, `world`, `cluster`, `host`, `remote-node`, `kube-apiserver`, `ingress`, `health`, `init`, and `unmanaged`. `internet` and `namespace` do not exist; use `world` for external traffic and label selectors for namespaces.",
    "example": "apiVersion: cilium.io/v2\nkind: CiliumNetworkPolicy\nmetadata:\n  name: allow-apiserver-and-world\nspec:\n  endpointSelector: {matchLabels: {app: controller}}\n  egress:\n  - toEntities: [kube-apiserver]\n  - toEntities: [world]\n    toPorts: [{ports: [{port: \"443\", protocol: TCP}]}]",
    "diagram": "flowchart TD\n    E[\"Cilium entities\"] --> W[\"world: outside the cluster\"]\n    E --> C[\"cluster: all cluster endpoints\"]\n    E --> H[\"host: local node\"]\n    E --> R[\"remote-node: other nodes\"]\n    E --> K[\"kube-apiserver\"]\n    E --> A[\"all\"]",
    "reference": "https://docs.cilium.io/en/stable/security/policy/language/#entities-based",
    "tags": [
      "cca",
      "network-policy",
      "entities"
    ],
    "id": "cilium-009",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "fill",
    "prompt": "Which Cilium resource `kind` lets you write a network policy that is not bound to a namespace and can select endpoints across the whole cluster, including node (host) endpoints via `nodeSelector`?",
    "answer": [
      "CiliumClusterwideNetworkPolicy",
      "ccnp",
      "ciliumclusterwidenetworkpolicy"
    ],
    "pattern": "^(ciliumclusterwidenetworkpolicy|ccnp)$",
    "explanation": "`CiliumClusterwideNetworkPolicy` (short name `ccnp`) is the cluster-scoped counterpart of `CiliumNetworkPolicy`. Because it has no namespace it can select endpoints everywhere, and it is the only policy type that can use `nodeSelector` to apply host firewall rules to nodes. Its rules are evaluated together with namespaced policies.",
    "example": "apiVersion: cilium.io/v2\nkind: CiliumClusterwideNetworkPolicy\nmetadata:\n  name: allow-dns-everywhere   # no namespace field\nspec:\n  endpointSelector: {}\n  egress:\n  - toEndpoints:\n    - matchLabels: {k8s:io.kubernetes.pod.namespace: kube-system, k8s-app: kube-dns}\n    toPorts: [{ports: [{port: \"53\", protocol: ANY}]}]\nkubectl get ccnp",
    "diagram": "flowchart LR\n    CNP[\"CiliumNetworkPolicy\"] -->|\"namespaced\"| NS[\"endpoints in one namespace\"]\n    CCNP[\"CiliumClusterwideNetworkPolicy\"] -->|\"cluster scoped\"| ALL[\"endpoints in every namespace\"]\n    CCNP -->|\"nodeSelector\"| NODE[\"host endpoints / host firewall\"]\n    NS --> V[\"combined policy verdict\"]\n    ALL --> V",
    "reference": "https://docs.cilium.io/en/stable/security/policy/#ciliumclusterwidenetworkpolicy",
    "tags": [
      "cca",
      "network-policy",
      "clusterwide"
    ],
    "id": "cilium-010",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "Cilium runs with the default policy enforcement mode. A `CiliumNetworkPolicy` that contains only an `ingress` section is applied to pods labelled `app=db`. What happens to egress traffic from those pods?",
    "options": [
      "Egress stays fully allowed; only ingress becomes default-deny because enforcement is applied per direction",
      "Egress becomes default-deny because any policy selecting an endpoint denies both directions",
      "Egress is denied only to endpoints outside the namespace",
      "Egress is audited but not dropped"
    ],
    "optionNotes": [
      "Correct. In `default` mode an endpoint switches to default-deny independently for ingress and egress, and only for the direction that at least one selecting policy defines rules for.",
      "This describes what would happen in `always` mode, where every endpoint is default-deny in both directions regardless of policies.",
      "There is no namespace-boundary special case; either the direction is enforced or it is not.",
      "Audit behaviour only applies when the endpoint's policy audit mode is enabled; it is not the default."
    ],
    "answer": 0,
    "explanation": "Cilium supports three enforcement modes: `default` (enforce only on endpoints selected by a policy, per direction), `always` (all endpoints default-deny in both directions), and `never` (no enforcement). With `default`, an ingress-only policy makes ingress default-deny for the selected pods while their egress remains unrestricted, which is a common source of surprise when a later egress rule suddenly blocks DNS.",
    "example": "# check the mode\nkubectl -n kube-system exec ds/cilium -- cilium-dbg config | grep PolicyEnforcement\n# PolicyEnforcement   default\nkubectl -n kube-system exec ds/cilium -- cilium-dbg endpoint list\n# ENDPOINT  POLICY (ingress)  POLICY (egress)  IDENTITY  LABELS\n# 512       Enabled           Disabled         13251     k8s:app=db",
    "diagram": "flowchart TD\n    M[\"policy enforcement mode\"] --> D[\"default\"]\n    M --> A[\"always\"]\n    M --> N[\"never\"]\n    D --> I[\"ingress rule exists -> ingress default-deny\"]\n    D --> E[\"no egress rule -> egress allowed\"]\n    A --> B[\"both directions default-deny\"]\n    N --> O[\"nothing enforced\"]",
    "reference": "https://docs.cilium.io/en/stable/security/policy/intro/#policy-enforcement-modes",
    "tags": [
      "cca",
      "network-policy",
      "enforcement-modes"
    ],
    "id": "cilium-011",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "You install Cilium with `--set gatewayAPI.enabled=true --set kubeProxyReplacement=true`, but every `Gateway` you create stays without an address and the `GatewayClass` named `cilium` never appears. What is the most likely cause?",
    "options": [
      "The Gateway API CRDs were not installed before Cilium, so the operator could not register the GatewayClass",
      "Cilium Ingress (`ingressController.enabled`) must also be enabled for Gateway API to work",
      "An Istio control plane is required because Cilium delegates Gateway API to Istio",
      "Hubble must be enabled because the Gateway controller publishes addresses through Hubble Relay"
    ],
    "optionNotes": [
      "Correct. Cilium does not ship the Gateway API CRDs; they must be applied from the gateway-api release (standard channel plus, for TLSRoute, the experimental CRDs) before installation, otherwise the controller is not started.",
      "Cilium Ingress and Gateway API are independent features; both use the same embedded Envoy but neither requires the other.",
      "Cilium implements Gateway API natively through its own Envoy-based controller and does not depend on Istio.",
      "Hubble is observability only and has no role in Gateway address assignment."
    ],
    "answer": 0,
    "explanation": "Cilium's Gateway API support requires the Gateway API CRDs to be pre-installed and kube-proxy replacement (or at least NodePort/socket LB) to be enabled; when the CRDs are missing the operator skips the Gateway controller and no `cilium` GatewayClass is created. Cilium Ingress, which uses `ingressClassName: cilium`, is a separate feature built on the same embedded Envoy.",
    "example": "kubectl apply -f https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.0/standard-install.yaml\ncilium install --set kubeProxyReplacement=true --set gatewayAPI.enabled=true\nkubectl get gatewayclass\n# NAME     CONTROLLER                     ACCEPTED\n# cilium   io.cilium/gateway-controller   True",
    "diagram": "flowchart LR\n    CRD[\"Gateway API CRDs installed\"] --> OP[\"cilium-operator starts Gateway controller\"]\n    OP --> GC[\"GatewayClass cilium\"]\n    GW[\"Gateway + HTTPRoute\"] --> OP\n    OP -->|\"creates\"| SVC[\"LoadBalancer Service\"]\n    OP -->|\"programs\"| ENV[\"embedded Envoy\"]\n    ENV --> BE[\"backend Pods\"]",
    "reference": "https://docs.cilium.io/en/stable/network/servicemesh/gateway-api/gateway-api/",
    "tags": [
      "cca",
      "service-mesh",
      "gateway-api"
    ],
    "id": "cilium-012",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "Cilium's mutual authentication feature requires injecting an Envoy sidecar into every participating Pod so that each connection is terminated with mTLS inside the Pod.",
    "answer": false,
    "explanation": "Cilium mutual authentication is sidecar-free. Identities are issued as SPIFFE SVIDs by a SPIRE server, and when a policy sets `authentication.mode: required` the cilium-agents on the two nodes perform a one-time mTLS handshake with each other on behalf of the endpoints; the result is cached in an eBPF auth map and subsequent packets are checked in-kernel. No per-pod proxy is injected and the application traffic itself is not terminated by a sidecar.",
    "example": "cilium install --set authentication.mutual.spire.enabled=true \\\n  --set authentication.mutual.spire.install.enabled=true\n---\napiVersion: cilium.io/v2\nkind: CiliumNetworkPolicy\nmetadata: {name: require-mtls}\nspec:\n  endpointSelector: {matchLabels: {app: backend}}\n  ingress:\n  - fromEndpoints: [{matchLabels: {app: frontend}}]\n    authentication:\n      mode: required",
    "diagram": "sequenceDiagram\n    participant A as cilium-agent node A\n    participant S as SPIRE server\n    participant B as cilium-agent node B\n    A->>S: fetch SVID for identity frontend\n    B->>S: fetch SVID for identity backend\n    A->>B: mTLS handshake agent to agent\n    B-->>A: verified\n    A->>A: write entry to eBPF auth map\n    Note over A,B: pod packets checked in kernel, no sidecar",
    "reference": "https://docs.cilium.io/en/stable/network/servicemesh/mutual-authentication/mutual-authentication/",
    "tags": [
      "cca",
      "service-mesh",
      "mutual-auth"
    ],
    "id": "cilium-013",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "You want to see only the flows in namespace `shop` that Cilium dropped, for example because of network policy. Which command does this?",
    "options": [
      "`hubble observe --namespace shop --verdict DROPPED`",
      "`hubble observe --namespace shop --drop`",
      "`cilium monitor --verdict DROPPED --namespace shop`",
      "`hubble status --namespace shop --dropped`"
    ],
    "optionNotes": [
      "Correct. `hubble observe` accepts `--verdict` with values such as `FORWARDED`, `DROPPED`, `AUDIT`, `REDIRECTED` and `ERROR`, combined with namespace/pod/label filters.",
      "There is no `--drop` flag on `hubble observe`; verdict filtering uses `--verdict`.",
      "`cilium monitor` (run inside an agent pod) is a low-level per-node datapath trace with `--type drop`; it has no `--verdict` or `--namespace` flags.",
      "`hubble status` only reports flow counters and node coverage; it does not list flows."
    ],
    "answer": 0,
    "explanation": "Hubble assigns a verdict to each flow: `FORWARDED`, `DROPPED`, `AUDIT` (would have been dropped but audit mode is on), `REDIRECTED` (sent to a proxy), `TRACED`, `TRANSLATED` and `ERROR`. Filtering with `--verdict DROPPED` plus `--namespace` is the standard way to debug policy denials; the output also shows the drop reason such as `Policy denied`.",
    "example": "cilium hubble port-forward &\nhubble observe --namespace shop --verdict DROPPED --follow\n# Jan 10 09:12:01.123: shop/cart-7d9 (ID:4211) <> shop/db-5c1 (ID:1877) Policy denied DROPPED (TCP Flags: SYN)\nhubble observe --pod shop/cart-7d9 --type l7   # L7 (HTTP/DNS) flows",
    "diagram": "flowchart LR\n    F[\"flow event from eBPF\"] --> V{\"verdict\"}\n    V --> FW[\"FORWARDED\"]\n    V --> DR[\"DROPPED with reason\"]\n    V --> AU[\"AUDIT\"]\n    V --> RE[\"REDIRECTED to proxy\"]\n    DR --> H[\"hubble observe --verdict DROPPED\"]",
    "reference": "https://docs.cilium.io/en/stable/observability/hubble/hubble-cli/",
    "tags": [
      "cca",
      "hubble",
      "observability"
    ],
    "id": "cilium-014",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "multi",
    "prompt": "Which statements about Hubble's architecture are correct? (Select all that apply.)",
    "options": [
      "The Hubble server runs embedded inside each cilium-agent and exposes that node's flows",
      "Hubble Relay aggregates flows from all nodes so `hubble observe` can show cluster-wide traffic",
      "Hubble UI renders a service map built from Hubble Relay's flow data",
      "Hubble persists all flows in an embedded database for long-term retention",
      "Hubble requires Prometheus to be installed before it can be enabled"
    ],
    "optionNotes": [
      "Correct: every agent runs a Hubble server backed by an in-memory ring buffer of recent flows, reachable via a local unix socket or a gRPC port.",
      "Correct: `hubble-relay` connects to every agent's Hubble server and exposes one gRPC endpoint that the CLI and UI use for a cluster-wide view.",
      "Correct: `hubble-ui` is a web front end that queries Relay to draw the service dependency graph and flow table.",
      "Flows are kept in a per-node ring buffer only; for retention you export them with the Hubble exporter to files or ship metrics to Prometheus.",
      "Prometheus is optional; Hubble can export metrics to it but does not depend on it."
    ],
    "answer": [
      0,
      1,
      2
    ],
    "explanation": "Hubble is built into cilium-agent: each node holds recent flows in a ring buffer and serves them over gRPC. Hubble Relay fans out to every node for a cluster-wide API, and Hubble UI visualises the resulting service map. Nothing is persisted by default, and Prometheus integration is optional.",
    "example": "cilium hubble enable --ui\ncilium status | grep -i hubble\n# Hubble Relay:       OK\ncilium hubble port-forward &\nhubble status\n# Healthcheck (via localhost:4245): Ok\n# Current/Max Flows: 12,285/12,285 (100.00%)\n# Flows/s: 41.52\ncilium hubble ui",
    "diagram": "flowchart LR\n    A1[\"cilium-agent node 1 with Hubble server\"] --> R[\"hubble-relay\"]\n    A2[\"cilium-agent node 2 with Hubble server\"] --> R\n    R --> CLI[\"hubble CLI\"]\n    R --> UI[\"hubble-ui service map\"]\n    A1 -.->|\"optional metrics\"| P[\"Prometheus\"]",
    "reference": "https://docs.cilium.io/en/stable/observability/hubble/",
    "tags": [
      "cca",
      "hubble",
      "architecture"
    ],
    "id": "cilium-015",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "Two Kubernetes clusters are joined with Cilium Cluster Mesh. A Service named `api` exists in both clusters, but clients in cluster 1 are only ever load-balanced to local backends. What must be added for backends in cluster 2 to be included?",
    "options": [
      "Annotate the Service with `service.cilium.io/global: \"true\"` in both clusters",
      "Set `spec.type: ExternalName` pointing to the other cluster's Service",
      "Create a `CiliumClusterwideNetworkPolicy` allowing `remote-node`",
      "Run `cilium clustermesh connect` again with `--enable-global-services`"
    ],
    "optionNotes": [
      "Correct. Global services are opt-in: the Service must exist with the same name and namespace in each cluster and carry the `service.cilium.io/global: \"true\"` annotation so the agents merge backends from the shared clustermesh-apiserver state.",
      "`ExternalName` returns a CNAME and would bypass Cilium's cross-cluster load balancing entirely.",
      "Network policy controls whether traffic is allowed, not which backends a Service has; `remote-node` refers to other nodes, not other clusters.",
      "There is no such flag; global service behaviour is controlled per Service by annotation."
    ],
    "answer": 0,
    "explanation": "Cluster Mesh connects clusters through each cluster's `clustermesh-apiserver`, giving every agent a view of remote identities, nodes and services. Prerequisites include unique cluster names/IDs, non-overlapping PodCIDRs and node reachability. Services are merged only when annotated as global; `service.cilium.io/affinity: local` can then prefer local backends while still failing over remotely.",
    "example": "cilium clustermesh enable --context c1 --service-type LoadBalancer\ncilium clustermesh enable --context c2 --service-type LoadBalancer\ncilium clustermesh connect --context c1 --destination-context c2\ncilium clustermesh status --context c1 --wait\nkubectl annotate svc api service.cilium.io/global=\"true\" --context c1\nkubectl annotate svc api service.cilium.io/global=\"true\" --context c2",
    "diagram": "flowchart LR\n    subgraph C1[\"cluster 1\"]\n        A1[\"cilium-agent\"] --> S1[\"Service api global=true\"]\n        M1[\"clustermesh-apiserver\"]\n    end\n    subgraph C2[\"cluster 2\"]\n        A2[\"cilium-agent\"] --> S2[\"Service api global=true\"]\n        M2[\"clustermesh-apiserver\"]\n    end\n    A1 -->|\"reads remote state\"| M2\n    A2 -->|\"reads remote state\"| M1\n    S1 -->|\"backends from both clusters\"| B[\"merged endpoint list\"]",
    "reference": "https://docs.cilium.io/en/stable/network/clustermesh/services/",
    "tags": [
      "cca",
      "cluster-mesh",
      "global-services"
    ],
    "id": "cilium-016",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which statements about transparent encryption in Cilium are correct? (Select all that apply.)",
    "options": [
      "With WireGuard, each agent generates its node key pair automatically and publishes the public key through its `CiliumNode` resource",
      "IPsec mode requires a pre-shared key stored in a Kubernetes Secret named `cilium-ipsec-keys` in the Cilium namespace",
      "Pod-to-pod encryption is enabled by default in a standard Helm installation",
      "When WireGuard is active, `cilium status` reports `Encryption: Wireguard` and the node has a `cilium_wg0` interface",
      "WireGuard encryption requires Envoy because the handshake is performed at L7"
    ],
    "optionNotes": [
      "Correct: WireGuard needs no key management by the operator; the agent creates keys and peers discover each other via `CiliumNode` annotations.",
      "Correct: for IPsec you create the `cilium-ipsec-keys` Secret with an SPI, algorithm and key before enabling `encryption.type=ipsec`, and rotate it by bumping the SPI.",
      "Encryption is off by default; it must be enabled with `encryption.enabled=true` and a type.",
      "Correct: the agent creates the `cilium_wg0` device and `cilium status`/`cilium-dbg status` show the encryption mode and peer count.",
      "WireGuard operates at L3 in the kernel; it has nothing to do with Envoy or L7."
    ],
    "answer": [
      0,
      1,
      3
    ],
    "explanation": "Cilium offers two transparent encryption backends. WireGuard is the simplest: keys are auto-generated per node and exchanged through `CiliumNode`, and traffic between pods on different nodes goes through `cilium_wg0`. IPsec uses kernel XFRM with a user-supplied pre-shared key in the `cilium-ipsec-keys` Secret and supports key rotation. Both are opt-in and operate entirely below L7.",
    "example": "cilium install --set encryption.enabled=true --set encryption.type=wireguard\ncilium status | grep Encryption\n# Encryption: Wireguard   [NodeEncryption: Disabled, cilium_wg0 (Pubkey: ..., Port: 51871, Peers: 2)]\n\n# IPsec alternative\nkubectl -n kube-system create secret generic cilium-ipsec-keys \\\n  --from-literal=keys=\"3+ rfc4106(gcm(aes)) $(xxd -p -l 20 /dev/urandom) 128\"",
    "diagram": "flowchart LR\n    P1[\"Pod on node A\"] --> E1[\"eBPF datapath\"]\n    E1 -->|\"cilium_wg0 or XFRM\"| ENC[\"encrypted tunnel\"]\n    ENC --> E2[\"eBPF datapath node B\"]\n    E2 --> P2[\"Pod on node B\"]\n    K[\"WireGuard: auto keys via CiliumNode\"] --> ENC\n    S[\"IPsec: cilium-ipsec-keys Secret\"] --> ENC",
    "reference": "https://docs.cilium.io/en/stable/security/network/encryption/",
    "tags": [
      "cca",
      "encryption",
      "wireguard"
    ],
    "id": "cilium-017",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "Cilium's BGP Control Plane can only advertise PodCIDRs and LoadBalancer IPs if MetalLB is installed alongside it to speak BGP to the top-of-rack routers.",
    "answer": false,
    "explanation": "Cilium ships its own BGP implementation (based on GoBGP) inside cilium-agent, enabled with `bgpControlPlane.enabled=true`. Peers and advertisements are declared with `CiliumBGPClusterConfig`, `CiliumBGPPeerConfig` and `CiliumBGPAdvertisement` resources, and LoadBalancer IPs come from Cilium's own LB IPAM (`CiliumLoadBalancerIPPool`). MetalLB is not required and would conflict with Cilium speaking BGP for the same prefixes.",
    "example": "apiVersion: cilium.io/v2alpha1\nkind: CiliumBGPClusterConfig\nmetadata: {name: rack1}\nspec:\n  nodeSelector: {matchLabels: {rack: \"1\"}}\n  bgpInstances:\n  - name: instance-65001\n    localASN: 65001\n    peers:\n    - name: tor\n      peerASN: 65000\n      peerAddress: 192.168.1.1\n      peerConfigRef: {name: tor-peer}\nkubectl -n kube-system exec ds/cilium -- cilium-dbg bgp peers",
    "diagram": "flowchart LR\n    CC[\"CiliumBGPClusterConfig\"] --> AG[\"cilium-agent GoBGP speaker\"]\n    AD[\"CiliumBGPAdvertisement PodCIDR + LB IPs\"] --> AG\n    POOL[\"CiliumLoadBalancerIPPool\"] --> AD\n    AG -->|\"BGP session\"| TOR[\"top-of-rack router\"]\n    TOR --> NET[\"rest of the network\"]",
    "reference": "https://docs.cilium.io/en/stable/network/bgp-control-plane/",
    "tags": [
      "cca",
      "bgp",
      "networking"
    ],
    "id": "cilium-018",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "A payments Pod must reach an external bank API whose firewall allows a single fixed source IP. Which Cilium feature makes all egress from the selected Pods leave the cluster from one designated node with a specific source address?",
    "options": [
      "Egress Gateway with a `CiliumEgressGatewayPolicy` selecting the pods, a destination CIDR and the gateway node's `egressIP`",
      "A `CiliumNetworkPolicy` with `toCIDR` for the bank's address range",
      "Cluster Mesh with the bank API registered as a global service",
      "Enabling BGP so the Pod IP is advertised to the bank"
    ],
    "optionNotes": [
      "Correct. The Egress Gateway feature SNATs matching traffic on a chosen gateway node to a stable IP; it needs kube-proxy replacement and BPF masquerading, and works with both tunnel and native routing.",
      "`toCIDR` only allows or denies traffic; it does not change where packets exit or what source IP they carry.",
      "Cluster Mesh joins Kubernetes clusters; it cannot import an external SaaS endpoint as a service.",
      "Advertising PodCIDRs would expose ephemeral pod IPs, which cannot be allow-listed as a single fixed address."
    ],
    "answer": 0,
    "explanation": "Egress Gateway redirects egress traffic from selected pods to a gateway node, where it is masqueraded to a predictable `egressIP` before leaving the cluster; the policy chooses pods with `selectors.podSelector`, destinations with `destinationCIDRs` and the node with `egressGateway.nodeSelector`. Because it relies on the eBPF datapath, it requires `kubeProxyReplacement=true` and `bpf.masquerade=true`.",
    "example": "cilium install --set egressGateway.enabled=true --set bpf.masquerade=true --set kubeProxyReplacement=true\n---\napiVersion: cilium.io/v2\nkind: CiliumEgressGatewayPolicy\nmetadata: {name: bank-egress}\nspec:\n  selectors:\n  - podSelector: {matchLabels: {app: payments}}\n  destinationCIDRs: [\"203.0.113.0/24\"]\n  egressGateway:\n    nodeSelector: {matchLabels: {egress-gw: \"true\"}}\n    egressIP: 198.51.100.10",
    "diagram": "flowchart LR\n    P[\"payments Pod on node A\"] -->|\"dst 203.0.113.0/24\"| E[\"eBPF: egress policy match\"]\n    E -->|\"tunnel to gateway\"| G[\"gateway node with egress-gw label\"]\n    G -->|\"SNAT to 198.51.100.10\"| B[\"bank API firewall allow-list\"]\n    O[\"other traffic\"] -->|\"normal masquerade\"| W[\"world\"]",
    "reference": "https://docs.cilium.io/en/stable/network/egress-gateway/egress-gateway/",
    "tags": [
      "cca",
      "egress-gateway",
      "networking"
    ],
    "id": "cilium-019",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "fill",
    "prompt": "Which IPAM mode does Cilium use by default when installed with the `cilium` CLI or Helm on a generic (non-cloud-managed) cluster, where cilium-operator hands each node a PodCIDR from a Cilium-managed pool (`ipam.mode` value)?",
    "answer": [
      "cluster-pool",
      "clusterpool",
      "cluster pool"
    ],
    "pattern": "^cluster[\\s-]?pool$",
    "explanation": "`cluster-pool` is the default `ipam.mode`: the operator carves per-node PodCIDRs from `ipam.operator.clusterPoolIPv4PodCIDRList` and records them in `CiliumNode`. Other modes are `kubernetes` (use the PodCIDR assigned by the Kubernetes controller-manager), `multi-pool` (per-namespace/pod pools via `CiliumPodIPPool`), `crd` (externally managed), and cloud-specific `eni`, `azure` and `alibabacloud` modes that allocate VPC-native addresses.",
    "example": "cilium install --set ipam.mode=cluster-pool \\\n  --set ipam.operator.clusterPoolIPv4PodCIDRList=10.42.0.0/16 \\\n  --set ipam.operator.clusterPoolIPv4MaskSize=24\nkubectl -n kube-system exec ds/cilium -- cilium-dbg status | grep -A2 IPAM\n# IPAM:   IPv4: 5/254 allocated from 10.42.3.0/24",
    "diagram": "flowchart TD\n    M[\"ipam.mode\"] --> CP[\"cluster-pool default: operator carves node CIDRs\"]\n    M --> K8[\"kubernetes: use node.spec.podCIDR\"]\n    M --> MP[\"multi-pool: CiliumPodIPPool per workload\"]\n    M --> CL[\"eni / azure / alibabacloud: VPC-native IPs\"]\n    CP --> CN[\"CiliumNode.spec.ipam.podCIDRs\"]\n    CN --> AG[\"agent allocates pod IPs\"]",
    "reference": "https://docs.cilium.io/en/stable/network/concepts/ipam/",
    "tags": [
      "cca",
      "ipam",
      "installation"
    ],
    "id": "cilium-020",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "Cilium is installed with `routingMode=native` on a cluster where nodes sit in different L3 subnets and no BGP or cloud route integration is configured. Pods on different nodes cannot reach each other, while Pods on the same node can. What is the most likely cause?",
    "options": [
      "In native routing mode Cilium does not encapsulate traffic, so the underlying network must be able to route PodCIDRs between nodes; nothing is installing those routes",
      "Native routing mode requires `kubeProxyReplacement=true`, which was not enabled",
      "Native routing only works when all nodes share a single IPAM pool of /16 or larger",
      "The VXLAN port 8472 is blocked between the subnets"
    ],
    "optionNotes": [
      "Correct. With `routingMode=native` Cilium sends Pod packets unencapsulated and relies on the network to route each node's PodCIDR. Across L3 subnets you need BGP, cloud routes, or `autoDirectNodeRoutes` on an L2 network.",
      "kube-proxy replacement affects Service load balancing, not whether Pod-to-Pod packets can be routed between nodes.",
      "The IPAM pool size has no effect on inter-node reachability; the missing piece is routing information in the underlay.",
      "VXLAN is only used in tunnel mode. In native mode there is no encapsulation, so a blocked UDP 8472 is irrelevant."
    ],
    "answer": 0,
    "explanation": "In tunnel mode (VXLAN or Geneve) Cilium encapsulates Pod traffic between nodes, so the underlay only needs node-to-node reachability. In native routing mode the underlay must know how to reach every PodCIDR, which requires BGP, cloud provider routes, or `autoDirectNodeRoutes=true` when all nodes share one L2 segment.",
    "example": "# tunnel mode (default) - works on any network with node reachability\ncilium install --set routingMode=tunnel --set tunnelProtocol=vxlan\n\n# native routing on a single L2 segment\ncilium install --set routingMode=native \\\n  --set autoDirectNodeRoutes=true \\\n  --set ipv4NativeRoutingCIDR=10.0.0.0/8",
    "diagram": "flowchart LR\n    P1[\"Pod A on node 1\"] --> D{\"routingMode\"}\n    D -->|\"tunnel\"| V[\"VXLAN or Geneve encapsulation\"]\n    V --> N2[\"node 2 decapsulates\"]\n    D -->|\"native\"| R[\"plain IP packet\"]\n    R --> U[\"underlay must route PodCIDR\"]\n    U -->|\"BGP, cloud routes, autoDirectNodeRoutes\"| N2\n    N2 --> P2[\"Pod B\"]",
    "reference": "https://docs.cilium.io/en/stable/network/concepts/routing/",
    "tags": [
      "cca",
      "routing",
      "native-routing"
    ],
    "id": "cilium-021",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "fill",
    "prompt": "Cilium support asked you to collect logs, eBPF map dumps, CRDs and Hubble flows from every node into a single zip file for troubleshooting. Which `cilium` CLI subcommand does this? Give the full two-word command.",
    "answer": [
      "cilium sysdump"
    ],
    "pattern": "^cilium\\s+sysdump$",
    "explanation": "`cilium sysdump` gathers agent and operator logs, `cilium-dbg` output, BPF map contents, Kubernetes resources and Hubble flows from the cluster and writes them to a timestamped `cilium-sysdump-*.zip` archive that you can attach to a bug report.",
    "example": "cilium sysdump --output-filename cilium-sysdump-prod\n# collects: logs, cilium-dbg status/endpoint/policy output, bpf maps, CRDs, hubble flows\n# writes ./cilium-sysdump-prod.zip",
    "diagram": "flowchart TD\n    S[\"cilium sysdump\"] --> L[\"agent and operator logs\"]\n    S --> B[\"eBPF map dumps\"]\n    S --> K[\"Kubernetes objects and CRDs\"]\n    S --> H[\"Hubble flows\"]\n    L --> Z[\"cilium-sysdump-timestamp.zip\"]\n    B --> Z\n    K --> Z\n    H --> Z",
    "reference": "https://docs.cilium.io/en/stable/operations/troubleshooting/",
    "tags": [
      "cca",
      "troubleshooting",
      "cli"
    ],
    "id": "cilium-022",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "Running `cilium-dbg endpoint list` inside an agent Pod shows one endpoint stuck in the `waiting-for-identity` state for several minutes. What does this state mean?",
    "options": [
      "The agent has not yet been able to allocate a numeric security identity for the endpoint's label set, usually because the identity allocation backend (CRDs or kvstore) is unreachable",
      "The endpoint is waiting for its Pod to pass its Kubernetes readiness probe",
      "The endpoint is queued behind other endpoints while its eBPF program is being compiled",
      "The Pod has no labels, so Cilium cannot assign an identity until labels are added"
    ],
    "optionNotes": [
      "Correct. Identities are allocated from labels via CRDs (`CiliumIdentity`) or a kvstore; if that allocation fails or times out the endpoint sits in `waiting-for-identity` and its datapath is not fully programmed.",
      "Cilium endpoint states are independent of Kubernetes readiness probes.",
      "That situation is represented by the `regenerating` or `waiting-to-regenerate` states, not `waiting-for-identity`.",
      "A Pod always carries at least the namespace and default labels, so an identity can be allocated; missing labels are not the cause."
    ],
    "answer": 0,
    "explanation": "Cilium endpoints move through states such as `creating`, `waiting-for-identity`, `waiting-to-regenerate`, `regenerating`, `ready`, `disconnecting` and `disconnected`. `waiting-for-identity` indicates the agent is still trying to resolve the endpoint's labels into a numeric identity, typically because the CRD or kvstore backend is slow or unavailable.",
    "example": "kubectl -n kube-system exec ds/cilium -- cilium-dbg endpoint list\n# ENDPOINT   POLICY (ingress)   POLICY (egress)   IDENTITY   LABELS                 STATUS\n# 1234       Disabled           Disabled          <no id>    k8s:app=web            waiting-for-identity\n# 2345       Enabled            Disabled          21561      k8s:app=api            ready",
    "diagram": "flowchart LR\n    C[\"creating\"] --> W[\"waiting-for-identity\"]\n    W -->|\"identity allocated\"| G[\"waiting-to-regenerate\"]\n    G --> R[\"regenerating\"]\n    R --> Y[\"ready\"]\n    Y -->|\"pod deleted\"| D[\"disconnecting\"]\n    D --> X[\"disconnected\"]\n    W -->|\"CRD or kvstore unreachable\"| W",
    "reference": "https://docs.cilium.io/en/stable/operations/troubleshooting/",
    "tags": [
      "cca",
      "endpoints",
      "troubleshooting"
    ],
    "id": "cilium-023",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "You want to use `CiliumClusterwideNetworkPolicy` to restrict which sources may reach SSH on the Kubernetes nodes themselves, not on Pods. Which Cilium feature must be enabled, and how are the nodes selected?",
    "options": [
      "Enable `hostFirewall.enabled=true` and write a policy with `nodeSelector` instead of `endpointSelector`",
      "Enable `hostFirewall.enabled=true` and write a policy whose `endpointSelector` matches the node's `kubernetes.io/hostname` label",
      "Enable `hostServices.enabled=true` and select nodes with `toEntities: host`",
      "No extra feature is required; any `CiliumNetworkPolicy` with `fromEntities: cluster` also applies to node traffic"
    ],
    "optionNotes": [
      "Correct. The host firewall applies policy to the node's host endpoint; policies for it use `nodeSelector` in a `CiliumClusterwideNetworkPolicy` to pick nodes by their Node labels.",
      "`endpointSelector` matches Pod endpoints; the host endpoint is selected by `nodeSelector`.",
      "`hostServices` relates to socket-level service load balancing for host processes, not to policy enforcement on the node.",
      "By default network policy applies only to Pod endpoints. Without the host firewall, node traffic is not policed."
    ],
    "answer": 0,
    "explanation": "Cilium's host firewall extends policy enforcement to the node's own network namespace via a special host endpoint with the reserved `host` identity. Policies targeting nodes must be `CiliumClusterwideNetworkPolicy` objects using `nodeSelector`, and it is recommended to start in audit mode to avoid locking yourself out of SSH.",
    "example": "apiVersion: cilium.io/v2\nkind: CiliumClusterwideNetworkPolicy\nmetadata:\n  name: node-ssh\nspec:\n  nodeSelector:\n    matchLabels:\n      node-role.kubernetes.io/worker: \"\"\n  ingress:\n  - fromCIDR: [\"10.10.0.0/24\"]\n    toPorts:\n    - ports:\n      - port: \"22\"\n        protocol: TCP",
    "diagram": "flowchart LR\n    H[\"helm: hostFirewall.enabled=true\"] --> E[\"host endpoint per node (identity: host)\"]\n    P[\"CiliumClusterwideNetworkPolicy\"] -->|\"nodeSelector\"| E\n    T[\"traffic to node port 22\"] --> E\n    E -->|\"allowed CIDR\"| A[\"accept\"]\n    E -->|\"other source\"| D[\"drop\"]",
    "reference": "https://docs.cilium.io/en/stable/security/host-firewall/",
    "tags": [
      "cca",
      "host-firewall",
      "network-policy"
    ],
    "id": "cilium-024",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "When Cilium's Bandwidth Manager is enabled, adding the annotation `kubernetes.io/egress-bandwidth: 10M` to a Pod limits that Pod's egress rate using eBPF and EDT (Earliest Departure Time) instead of the traditional tc token-bucket filter.",
    "answer": true,
    "explanation": "The Bandwidth Manager (`bandwidthManager.enabled=true`) honors the standard `kubernetes.io/egress-bandwidth` Pod annotation and enforces it with eBPF-based EDT rate limiting and the fq qdisc, which scales better than the TBF-based bandwidth plugin. Ingress limiting via `kubernetes.io/ingress-bandwidth` is not implemented by the Bandwidth Manager.",
    "example": "cilium install --set bandwidthManager.enabled=true\n\napiVersion: v1\nkind: Pod\nmetadata:\n  name: uploader\n  annotations:\n    kubernetes.io/egress-bandwidth: \"10M\"",
    "diagram": "flowchart LR\n    A[\"Pod annotation egress-bandwidth: 10M\"] --> C[\"cilium-agent reads annotation\"]\n    C --> M[\"eBPF map: rate per endpoint\"]\n    P[\"Pod egress packets\"] --> E[\"eBPF sets EDT timestamp\"]\n    E --> M\n    E --> F[\"fq qdisc paces packets\"]\n    F --> N[\"NIC\"]",
    "reference": "https://docs.cilium.io/en/stable/network/kubernetes/bandwidth-manager/",
    "tags": [
      "cca",
      "bandwidth-manager",
      "qos"
    ],
    "id": "cilium-025",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "Hubble is not installed on a cluster, but you need to watch policy drops for a specific endpoint in real time on the node where it runs. Which command gives you a live stream of datapath events from inside the agent Pod?",
    "options": [
      "`cilium-dbg monitor --type drop`",
      "`cilium-dbg bpf ct list global`",
      "`cilium-dbg endpoint log <id>`",
      "`cilium-dbg policy get`"
    ],
    "optionNotes": [
      "Correct. `cilium-dbg monitor` streams events from the node's perf ring buffer; `--type drop` limits output to dropped packets, and `--related-to <endpoint id>` narrows it to one endpoint.",
      "This dumps the connection tracking table, a static snapshot rather than a live event stream.",
      "`endpoint log` shows the endpoint's state-change history, not packet-level events.",
      "`policy get` prints the current policy repository; it does not show what the datapath is dropping."
    ],
    "answer": 0,
    "explanation": "`cilium-dbg monitor` (formerly `cilium monitor`) reads the per-node event ring buffer used by the eBPF datapath and prints drops, traces, policy verdicts and debug messages. It is the low-level tool Hubble builds on, useful when Hubble is not deployed or when you need node-local detail.",
    "example": "kubectl -n kube-system exec -it ds/cilium -- cilium-dbg monitor --type drop --related-to 1234\n# xx drop (Policy denied) flow 0x... to endpoint 1234, identity 21561->16 : 10.0.1.5:43210 -> 10.0.2.9:8080 tcp SYN",
    "diagram": "flowchart LR\n    B[\"eBPF datapath\"] -->|\"drop, trace, policy-verdict events\"| R[\"perf ring buffer\"]\n    R --> M[\"cilium-dbg monitor\"]\n    R --> H[\"hubble (if enabled)\"]\n    M -->|\"--type drop\"| O[\"only drops\"]\n    M -->|\"--related-to id\"| E[\"one endpoint\"]",
    "reference": "https://docs.cilium.io/en/stable/operations/troubleshooting/",
    "tags": [
      "cca",
      "troubleshooting",
      "monitor"
    ],
    "id": "cilium-026",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "Before enforcing a new default-deny policy in production, you want Cilium to log which flows would be dropped without actually dropping them. Which mechanism provides this?",
    "options": [
      "Policy audit mode, enabled with `policyAuditMode=true` or per endpoint with `cilium-dbg endpoint config <id> PolicyAuditMode=Enabled`, which reports `audit` verdicts in Hubble instead of dropping",
      "Adding `spec.mode: dryRun` to the `CiliumNetworkPolicy` object",
      "Setting `policyEnforcementMode=never`, which logs denied flows while allowing them",
      "Running `hubble observe --verdict WOULD_DROP`, which simulates the policy against live flows"
    ],
    "optionNotes": [
      "Correct. In audit mode the datapath still evaluates policy but allows traffic that would be denied and emits an audit verdict, so you can inspect it with `hubble observe --verdict AUDIT` before switching enforcement on.",
      "There is no `mode: dryRun` field in the CNP spec; audit mode is an agent or endpoint setting, not a policy field.",
      "`never` simply disables enforcement; it does not evaluate or log what a policy would have denied.",
      "`WOULD_DROP` is not a Hubble verdict. Valid verdicts include FORWARDED, DROPPED, AUDIT, REDIRECTED and ERROR."
    ],
    "answer": 0,
    "explanation": "Policy audit mode keeps traffic flowing but marks flows that a policy would deny with the `AUDIT` verdict. It can be enabled cluster-wide via the `policyAuditMode` Helm value or per endpoint with `cilium-dbg endpoint config`, and is the recommended way to validate host firewall and default-deny policies before enforcing them.",
    "example": "# cluster-wide\ncilium install --set policyAuditMode=true\n# per endpoint\nkubectl -n kube-system exec ds/cilium -- cilium-dbg endpoint config 1234 PolicyAuditMode=Enabled\n# observe what would be dropped\nhubble observe --verdict AUDIT --namespace shop",
    "diagram": "flowchart LR\n    F[\"incoming flow\"] --> P{\"policy allows?\"}\n    P -->|\"yes\"| A[\"FORWARDED\"]\n    P -->|\"no\"| M{\"audit mode?\"}\n    M -->|\"enabled\"| U[\"AUDIT verdict, packet forwarded\"]\n    M -->|\"disabled\"| D[\"DROPPED\"]\n    U --> H[\"hubble observe --verdict AUDIT\"]",
    "reference": "https://docs.cilium.io/en/stable/security/policy-creation/",
    "tags": [
      "cca",
      "network-policy",
      "audit-mode"
    ],
    "id": "cilium-027",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "Tetragon is deployed alongside Cilium. Which custom resource do you create to tell Tetragon which kernel functions or syscalls to observe and optionally enforce on?",
    "options": [
      "`TracingPolicy`",
      "`CiliumNetworkPolicy` with a `tracing` section",
      "`TetragonRule`",
      "`CiliumClusterwideEnvoyConfig`"
    ],
    "optionNotes": [
      "Correct. `TracingPolicy` (and its namespaced variant `TracingPolicyNamespaced`) defines kprobes, tracepoints or uprobes plus selectors and optional `Sigkill` or `Override` actions.",
      "Tetragon has its own CRDs; `CiliumNetworkPolicy` has no tracing section.",
      "`TetragonRule` is not a real resource kind.",
      "`CiliumClusterwideEnvoyConfig` configures Envoy L7 proxies, unrelated to runtime security observability."
    ],
    "answer": 0,
    "explanation": "Tetragon is an eBPF-based runtime security and observability tool from the Cilium project. Its `TracingPolicy` CRD attaches eBPF hooks to kernel functions (kprobes), tracepoints or user-space functions, and can filter events and apply enforcement actions such as killing the offending process.",
    "example": "apiVersion: cilium.io/v1alpha1\nkind: TracingPolicy\nmetadata:\n  name: block-etc-shadow\nspec:\n  kprobes:\n  - call: security_file_open\n    syscall: false\n    args:\n    - index: 0\n      type: file\n    selectors:\n    - matchArgs:\n      - index: 0\n        operator: Prefix\n        values: [\"/etc/shadow\"]\n      matchActions:\n      - action: Sigkill",
    "diagram": "flowchart LR\n    T[\"TracingPolicy CRD\"] --> A[\"tetragon agent\"]\n    A -->|\"attaches\"| K[\"kprobe / tracepoint / uprobe\"]\n    K --> E[\"kernel event\"]\n    E --> S{\"selectors match?\"}\n    S -->|\"yes\"| O[\"emit event via tetra getevents\"]\n    S -->|\"yes + action\"| X[\"Sigkill or Override\"]",
    "reference": "https://tetragon.io/docs/concepts/tracing-policy/",
    "tags": [
      "cca",
      "tetragon",
      "runtime-security"
    ],
    "id": "cilium-028",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "fill",
    "prompt": "You are using Cilium's LoadBalancer IPAM instead of MetalLB. Which Cilium resource `kind` defines the range of IPs from which `Service` objects of type `LoadBalancer` get their external address?",
    "answer": [
      "CiliumLoadBalancerIPPool",
      "cilium load balancer ip pool"
    ],
    "pattern": "^cilium\\s*load\\s*balancer\\s*ip\\s*pool$",
    "explanation": "`CiliumLoadBalancerIPPool` is a cluster-scoped CRD holding one or more CIDRs or ranges in `spec.blocks`. The operator assigns an IP from a matching pool to each `LoadBalancer` Service; the IP still needs to be announced, for example via BGP or L2 announcements.",
    "example": "apiVersion: cilium.io/v2alpha1\nkind: CiliumLoadBalancerIPPool\nmetadata:\n  name: prod-pool\nspec:\n  blocks:\n  - cidr: 192.0.2.0/24\n  serviceSelector:\n    matchLabels:\n      env: prod",
    "diagram": "flowchart LR\n    S[\"Service type LoadBalancer\"] --> O[\"cilium-operator LB IPAM\"]\n    P[\"CiliumLoadBalancerIPPool\"] -->|\"blocks + serviceSelector\"| O\n    O -->|\"sets status.loadBalancer.ingress\"| S\n    S --> A{\"announce\"}\n    A --> B[\"BGP control plane\"]\n    A --> L[\"L2 announcements\"]",
    "reference": "https://docs.cilium.io/en/stable/network/lb-ipam/",
    "tags": [
      "cca",
      "lb-ipam",
      "services"
    ],
    "id": "cilium-029",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "single",
    "prompt": "You installed the `hubble` CLI on your laptop and run `hubble observe`, but it fails with a connection error to `localhost:4245`. What is the usual fix?",
    "options": [
      "Run `cilium hubble port-forward` so the `hubble-relay` service is reachable on localhost:4245",
      "Run `cilium hubble enable --ui`, since the CLI needs Hubble UI to be deployed",
      "Exec into a `cilium` agent Pod, because the `hubble` CLI only works from inside the cluster",
      "Set `hubble.relay.enabled=false` so the CLI talks to each node's Hubble server directly"
    ],
    "optionNotes": [
      "Correct. The CLI defaults to a Relay endpoint on localhost:4245; `cilium hubble port-forward` (or `kubectl port-forward svc/hubble-relay`) exposes it.",
      "Hubble UI is optional; the CLI only needs Relay, which `cilium hubble enable` deploys without `--ui`.",
      "Running `hubble observe` inside an agent Pod works for node-local flows, but the CLI is designed to be used from outside via Relay.",
      "Disabling Relay would remove the cluster-wide view; the CLI expects a Relay endpoint by default."
    ],
    "answer": 0,
    "explanation": "Hubble Relay aggregates flows from every node's Hubble server. The `hubble` CLI talks to Relay over gRPC on localhost:4245 by default, so `cilium hubble port-forward` (or an equivalent `kubectl port-forward`) is required unless Relay is exposed some other way.",
    "example": "cilium hubble enable --ui\ncilium hubble port-forward &\nhubble status\nhubble observe --namespace shop --follow",
    "diagram": "flowchart LR\n    C[\"hubble CLI on laptop\"] -->|\"localhost:4245\"| F[\"cilium hubble port-forward\"]\n    F --> R[\"hubble-relay service\"]\n    R --> N1[\"hubble server in agent node 1\"]\n    R --> N2[\"hubble server in agent node 2\"]\n    U[\"hubble-ui\"] --> R",
    "reference": "https://docs.cilium.io/en/stable/observability/hubble/setup/",
    "tags": [
      "cca",
      "hubble",
      "cli"
    ],
    "id": "cilium-030",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which statements about DNS-aware L7 rules in a `CiliumNetworkPolicy` are correct? (Select all that apply.)",
    "options": [
      "A `dns` rule under `toPorts.rules` uses `matchName` for exact names and `matchPattern` for wildcards such as `*.example.com`",
      "DNS rules are enforced by the Cilium DNS proxy running in the agent, not by Envoy",
      "A DNS rule automatically allows the resolved IPs on every port without an additional egress rule",
      "Typical `toFQDNs` policies pair a DNS rule allowing lookups to `kube-dns` with a separate `toFQDNs` egress rule for the actual traffic",
      "`matchPattern: \"*\"` allows the Pod to resolve any name while still recording the answers for `toFQDNs` use"
    ],
    "optionNotes": [
      "Correct. `matchName` requires an exact match; `matchPattern` supports `*` wildcards for a label or subdomain.",
      "Correct. Cilium redirects port 53 traffic to its built-in DNS proxy inside the agent; Envoy is used for HTTP, gRPC and Kafka.",
      "Wrong. A DNS rule only permits and inspects the lookup itself; you still need a `toFQDNs` (or CIDR) rule to allow the connection to the resolved IPs.",
      "Correct. The standard pattern is one egress rule to the DNS server with a `dns` L7 rule, plus a `toFQDNs` rule with the destination ports.",
      "Correct. A catch-all pattern is commonly used to observe DNS while still populating the FQDN-to-IP cache."
    ],
    "answer": [
      0,
      1,
      3,
      4
    ],
    "explanation": "DNS L7 rules govern which names a Pod may resolve and feed the DNS proxy's cache that `toFQDNs` rules rely on. They do not themselves authorize the follow-up connection, so a policy typically has two egress rules: one to the DNS server with `rules.dns`, and one with `toFQDNs` and the destination ports.",
    "example": "egress:\n- toEndpoints:\n  - matchLabels:\n      k8s:io.kubernetes.pod.namespace: kube-system\n      k8s:k8s-app: kube-dns\n  toPorts:\n  - ports:\n    - port: \"53\"\n      protocol: ANY\n    rules:\n      dns:\n      - matchPattern: \"*.github.com\"\n- toFQDNs:\n  - matchPattern: \"*.github.com\"\n  toPorts:\n  - ports:\n    - port: \"443\"\n      protocol: TCP",
    "diagram": "flowchart LR\n    P[\"Pod\"] -->|\"port 53\"| X[\"cilium DNS proxy\"]\n    X -->|\"matchName / matchPattern allowed?\"| K[\"kube-dns\"]\n    K --> X\n    X -->|\"record name to IP\"| C[\"FQDN cache\"]\n    C --> F[\"toFQDNs rule maps to IPs\"]\n    P -->|\"port 443\"| F\n    F --> E[\"external service\"]",
    "reference": "https://docs.cilium.io/en/stable/security/policy/language/",
    "tags": [
      "cca",
      "network-policy",
      "dns"
    ],
    "id": "cilium-031",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "In `hubble observe` output you see a flow from identity `6` to a Pod. Which reserved Cilium identity does the number 6 represent?",
    "options": [
      "`remote-node`, meaning the traffic originated from another node's host network namespace",
      "`host`, meaning the traffic originated from the local node",
      "`world`, meaning the traffic came from outside the cluster",
      "`kube-apiserver`, meaning the traffic came from the API server"
    ],
    "optionNotes": [
      "Correct. Reserved identities are fixed: `host` is 1, `world` is 2, `unmanaged` is 3, `health` is 4, `init` is 5, `remote-node` is 6 and `kube-apiserver` is 7.",
      "`host` is reserved identity 1.",
      "`world` is reserved identity 2.",
      "`kube-apiserver` is reserved identity 7."
    ],
    "answer": 0,
    "explanation": "Cilium reserves the low identity numbers for special entities: `host` (1), `world` (2), `unmanaged` (3), `health` (4), `init` (5), `remote-node` (6) and `kube-apiserver` (7). Regular Pod identities are allocated from 256 upward. Knowing these values helps interpret raw monitor and Hubble output.",
    "example": "kubectl -n kube-system exec ds/cilium -- cilium-dbg identity list\n# ID   LABELS\n# 1    reserved:host\n# 2    reserved:world\n# 3    reserved:unmanaged\n# 4    reserved:health\n# 5    reserved:init\n# 6    reserved:remote-node\n# 7    reserved:kube-apiserver",
    "diagram": "flowchart TD\n    R[\"reserved identities\"] --> H[\"1 host\"]\n    R --> W[\"2 world\"]\n    R --> U[\"3 unmanaged\"]\n    R --> E[\"4 health\"]\n    R --> I[\"5 init\"]\n    R --> N[\"6 remote-node\"]\n    R --> K[\"7 kube-apiserver\"]\n    R --> P[\"256 and above: Pod identities\"]",
    "reference": "https://docs.cilium.io/en/stable/security/policy/language/",
    "tags": [
      "cca",
      "identity",
      "hubble"
    ],
    "id": "cilium-032",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "boolean",
    "prompt": "Setting `autoDirectNodeRoutes=true` makes Cilium install a route for every remote node's PodCIDR on each node, which is sufficient for native routing between nodes even when the nodes are in different L3 subnets.",
    "answer": false,
    "explanation": "`autoDirectNodeRoutes` installs per-node PodCIDR routes with the remote node IP as next hop, which only works when all nodes are directly reachable on the same L2 network. Across L3 subnets the intermediate routers do not know the PodCIDRs, so you need BGP, cloud provider routes, or tunnel mode instead.",
    "example": "# fine on a single L2 segment\ncilium install --set routingMode=native --set autoDirectNodeRoutes=true \\\n  --set ipv4NativeRoutingCIDR=10.244.0.0/16\n\n# multiple L3 subnets: advertise PodCIDRs via BGP instead\ncilium install --set routingMode=native --set bgpControlPlane.enabled=true",
    "diagram": "flowchart LR\n    A[\"node 1: route 10.244.2.0/24 via node2-ip\"] --> Q{\"node 2 on same L2?\"}\n    Q -->|\"yes\"| OK[\"packet delivered directly\"]\n    Q -->|\"no, across router\"| RT[\"router has no PodCIDR route\"]\n    RT --> X[\"packet dropped\"]\n    RT -->|\"fix\"| B[\"BGP or cloud routes or tunnel\"]",
    "reference": "https://docs.cilium.io/en/stable/network/concepts/routing/",
    "tags": [
      "cca",
      "routing",
      "native-routing"
    ],
    "id": "cilium-033",
    "domain": "cilium"
  },
  {
    "difficulty": "easy",
    "type": "fill",
    "prompt": "With `hubble observe`, which flag restricts output to flows with a specific verdict, for example only dropped or only forwarded flows? Give the flag name including the leading dashes.",
    "answer": [
      "--verdict",
      "--verdict DROPPED",
      "--verdict=DROPPED"
    ],
    "pattern": "^--verdict(\\s*=?\\s*(DROPPED|FORWARDED|AUDIT|ERROR|REDIRECTED))?$",
    "explanation": "`hubble observe --verdict <VERDICT>` filters flows by the datapath decision. Valid verdicts include `FORWARDED`, `DROPPED`, `AUDIT`, `REDIRECTED` and `ERROR`. Combine it with `--namespace`, `--pod`, `--type` or `--protocol` to narrow the stream further.",
    "example": "hubble observe --verdict DROPPED --namespace shop --follow\n# Sep 12 10:01:02.123: shop/cart-6f8 (ID:21561) <> shop/db-0 (ID:13337) Policy denied DROPPED (TCP Flags: SYN)",
    "diagram": "flowchart LR\n    F[\"all flows from relay\"] --> V{\"--verdict\"}\n    V -->|\"DROPPED\"| D[\"policy or datapath drops\"]\n    V -->|\"FORWARDED\"| W[\"allowed flows\"]\n    V -->|\"AUDIT\"| A[\"would-be drops in audit mode\"]\n    D --> O[\"terminal output\"]\n    W --> O\n    A --> O",
    "reference": "https://docs.cilium.io/en/stable/observability/hubble/hubble-cli/",
    "tags": [
      "cca",
      "hubble",
      "cli"
    ],
    "id": "cilium-034",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "You enable Cilium's built-in Ingress controller with `ingressController.enabled=true` and create an `Ingress` object, but it never gets an address. Which of the following is the most common missing piece?",
    "options": [
      "The `Ingress` does not set `ingressClassName: cilium` (or the default class was not set with `ingressController.default=true`)",
      "The `Ingress` must reference an `IngressRoute` CRD instead of `rules`",
      "The Cilium Ingress controller only works when `routingMode=native`",
      "Envoy must be installed manually as a separate Deployment before Ingress can be used"
    ],
    "optionNotes": [
      "Correct. Cilium only reconciles Ingresses whose class is `cilium`, either explicitly via `ingressClassName` or because `ingressController.default=true` made it the cluster default.",
      "`IngressRoute` is a Traefik concept; Cilium uses the standard `Ingress` spec.",
      "The Ingress controller works in tunnel and native routing modes alike.",
      "Cilium deploys Envoy itself, either embedded in the agent or as the `cilium-envoy` DaemonSet; no manual install is required."
    ],
    "answer": 0,
    "explanation": "Cilium's Ingress controller requires kube-proxy replacement (or at least `nodePort.enabled=true`) and only handles `Ingress` objects with `ingressClassName: cilium`. The `loadbalancerMode` setting controls whether one shared LoadBalancer Service (`shared`) or one per Ingress (`dedicated`) is created, and that Service still needs an external IP from LB IPAM or the cloud.",
    "example": "cilium install --set kubeProxyReplacement=true \\\n  --set ingressController.enabled=true \\\n  --set ingressController.loadbalancerMode=dedicated\n\napiVersion: networking.k8s.io/v1\nkind: Ingress\nmetadata:\n  name: shop\nspec:\n  ingressClassName: cilium\n  rules:\n  - host: shop.example.com\n    http:\n      paths:\n      - path: /\n        pathType: Prefix\n        backend:\n          service:\n            name: shop\n            port:\n              number: 80",
    "diagram": "flowchart LR\n    I[\"Ingress ingressClassName: cilium\"] --> O[\"cilium-operator\"]\n    O -->|\"shared or dedicated\"| S[\"Service type LoadBalancer\"]\n    O --> C[\"CiliumEnvoyConfig\"]\n    C --> E[\"Envoy proxy\"]\n    S --> E\n    E --> B[\"backend Pods\"]",
    "reference": "https://docs.cilium.io/en/stable/network/servicemesh/ingress/",
    "tags": [
      "cca",
      "service-mesh",
      "ingress"
    ],
    "id": "cilium-035",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "multi",
    "prompt": "Which requirements must be satisfied before two clusters can be connected with Cilium Cluster Mesh? (Select all that apply.)",
    "options": [
      "Each cluster has a unique `cluster.name` and a unique numeric `cluster.id`",
      "PodCIDRs of the clusters must not overlap, unless you use overlapping-PodCIDR support with tunnel mode",
      "Both clusters must run the same Kubernetes distribution",
      "Nodes in each cluster can reach the other cluster's nodes and its `clustermesh-apiserver` endpoint",
      "Both clusters must use the same CA, for example by sharing the `cilium-ca` secret"
    ],
    "optionNotes": [
      "Correct. The cluster ID (1 to 255 by default) is encoded into security identities, and the name distinguishes clusters in Hubble and global services.",
      "Correct. Cross-cluster Pod routing needs non-overlapping ranges in the standard setup.",
      "Wrong. Cluster Mesh works across distributions and cloud providers; only network reachability and Cilium version compatibility matter.",
      "Correct. The agents in each cluster connect to the remote `clustermesh-apiserver` to learn identities, nodes and services, and Pods must be routable across clusters.",
      "Correct. `cilium clustermesh connect` relies on both sides trusting the same CA so the agents can authenticate to the remote apiserver over mTLS."
    ],
    "answer": [
      0,
      1,
      3,
      4
    ],
    "explanation": "Cluster Mesh requires distinct cluster names and IDs, non-overlapping PodCIDRs (in the default configuration), a shared CA for mTLS, and node-level reachability between clusters plus access to each cluster's `clustermesh-apiserver` Service. The CLI's `cilium clustermesh connect` automates exchanging the connection secrets.",
    "example": "cilium install --context c1 --set cluster.name=c1 --set cluster.id=1\ncilium install --context c2 --set cluster.name=c2 --set cluster.id=2\ncilium clustermesh enable --context c1 --service-type LoadBalancer\ncilium clustermesh enable --context c2 --service-type LoadBalancer\ncilium clustermesh connect --context c1 --destination-context c2\ncilium clustermesh status --context c1 --wait",
    "diagram": "flowchart LR\n    A[\"cluster c1 id=1\"] -->|\"mTLS, shared CA\"| B[\"clustermesh-apiserver in c2\"]\n    C[\"cluster c2 id=2\"] -->|\"mTLS, shared CA\"| D[\"clustermesh-apiserver in c1\"]\n    A -->|\"non-overlapping PodCIDR\"| C\n    B -->|\"identities, nodes, services\"| A\n    D -->|\"identities, nodes, services\"| C",
    "reference": "https://docs.cilium.io/en/stable/network/clustermesh/clustermesh/",
    "tags": [
      "cca",
      "cluster-mesh",
      "installation"
    ],
    "id": "cilium-036",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "single",
    "prompt": "Cilium runs in native routing mode. Pods can reach each other, but when a Pod connects to an external IP on the internet, its packets leave the node with the Pod IP as source and are never answered. Which setting is most likely misconfigured?",
    "options": [
      "`ipv4NativeRoutingCIDR` is set too broadly, so Cilium treats the destination as internal and skips SNAT (masquerading)",
      "`enableIPv4Masquerade=true` conflicts with native routing and must be turned off",
      "`kubeProxyReplacement` is disabled, so egress traffic is not masqueraded",
      "The `world` entity is missing from the Pod's egress policy"
    ],
    "optionNotes": [
      "Correct. Cilium masquerades traffic leaving the native routing CIDR. If that CIDR is `0.0.0.0/0` or otherwise includes the destination, packets are sent unmasqueraded with a Pod IP the external network cannot route back to.",
      "Masquerading is normally enabled in native routing mode precisely so external traffic uses the node IP; turning it off would make the symptom worse.",
      "Masquerading is performed by the eBPF or iptables masquerade logic regardless of kube-proxy replacement.",
      "A policy problem would show as a drop on the node; here the packet leaves the node, so policy is not the cause."
    ],
    "answer": 0,
    "explanation": "In native routing mode `ipv4NativeRoutingCIDR` defines the range that is reachable without NAT. Destinations outside it are masqueraded to the node IP (with eBPF masquerading when `bpf.masquerade=true`). Setting the CIDR too wide causes external traffic to keep the Pod source IP and fail.",
    "example": "# check the effective setting\nkubectl -n kube-system exec ds/cilium -- cilium-dbg config | grep -i -E 'NativeRouting|Masquerade'\n\n# correct: only the pod range is native\ncilium install --set routingMode=native \\\n  --set ipv4NativeRoutingCIDR=10.244.0.0/16 \\\n  --set bpf.masquerade=true",
    "diagram": "flowchart LR\n    P[\"Pod 10.244.1.5\"] --> D{\"destination inside ipv4NativeRoutingCIDR?\"}\n    D -->|\"yes\"| N[\"send with Pod IP source\"]\n    D -->|\"no\"| S[\"SNAT to node IP\"]\n    S --> I[\"internet replies to node\"]\n    N -->|\"if CIDR too broad\"| X[\"internet cannot route back to Pod IP\"]",
    "reference": "https://docs.cilium.io/en/stable/network/concepts/masquerading/",
    "tags": [
      "cca",
      "routing",
      "masquerading"
    ],
    "id": "cilium-037",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "single",
    "prompt": "Your Prometheus scrapes port 9962 on each Cilium agent and collects `cilium_*` metrics, but no `hubble_drop_total` or `hubble_flows_processed_total` series appear. What is missing?",
    "options": [
      "Hubble metrics must be enabled with `hubble.metrics.enabled` listing metric groups such as `drop` and `flow`, and they are served on the separate hubble-metrics port 9965",
      "Hubble metrics are only exported by `hubble-relay`, so Prometheus must scrape the Relay Pod instead of the agents",
      "The `cilium-operator` must be restarted so that it starts aggregating flow counters into its own metrics endpoint",
      "Hubble metrics require Hubble UI to be enabled because the UI backend computes them"
    ],
    "optionNotes": [
      "Correct. Hubble metrics are derived from flows and served on port 9965 per agent, configured via the `hubble.metrics.enabled` list; they are independent of the agent's own `cilium_*` metrics on 9962.",
      "Relay forwards flows to clients; it does not export the per-flow `hubble_*` metrics.",
      "The operator exposes its own `cilium_operator_*` metrics but does not aggregate flow metrics.",
      "Hubble UI is not involved in metric export."
    ],
    "answer": 0,
    "explanation": "Hubble metrics are derived from observed flows and are served on the `hubble-metrics` port (9965 by default) of each agent, distinct from the agent's `cilium_*` metrics on port 9962. The list of enabled metric groups is configured through the `hubble.metrics.enabled` Helm value, and options like `labelsContext` control cardinality.",
    "example": "cilium install \\\n  --set hubble.enabled=true \\\n  --set hubble.metrics.enableOpenMetrics=true \\\n  --set \"hubble.metrics.enabled={dns,drop,tcp,flow,port-distribution,icmp,httpV2:exemplars=true;labelsContext=source_ip\\,source_namespace\\,destination_namespace}\"\n# scrape :9965/metrics for hubble_flows_processed_total, hubble_drop_total ...",
    "diagram": "flowchart LR\n    B[\"eBPF datapath events\"] --> H[\"hubble server in agent\"]\n    H -->|\"flows\"| R[\"hubble-relay\"]\n    H -->|\"aggregated by enabled groups\"| M[\"hubble_* metrics :9965\"]\n    A[\"cilium-agent internals\"] --> C[\"cilium_* metrics :9962\"]\n    M --> P[\"Prometheus\"]\n    C --> P",
    "reference": "https://docs.cilium.io/en/stable/observability/metrics/",
    "tags": [
      "cca",
      "hubble",
      "metrics"
    ],
    "id": "cilium-038",
    "domain": "cilium"
  },
  {
    "difficulty": "hard",
    "type": "multi",
    "prompt": "Which Cilium features cause traffic to be redirected through an Envoy proxy rather than being handled entirely in the eBPF datapath? (Select all that apply.)",
    "options": [
      "HTTP L7 rules in a `CiliumNetworkPolicy`",
      "DNS `matchPattern` rules in a `CiliumNetworkPolicy`",
      "The Cilium Ingress controller and Gateway API implementation",
      "Kafka L7 rules in a `CiliumNetworkPolicy`",
      "ClusterIP load balancing with kube-proxy replacement"
    ],
    "optionNotes": [
      "Correct. HTTP-aware policy needs request parsing, which is done by Envoy with Cilium's policy filter.",
      "Wrong. DNS rules are enforced by the agent's built-in DNS proxy, not Envoy.",
      "Correct. Ingress and Gateway API resources are translated into `CiliumEnvoyConfig` objects and served by Envoy.",
      "Correct. Kafka protocol parsing is implemented as an Envoy filter.",
      "Wrong. Service load balancing happens in eBPF (socket-level or tc-level) without any proxy."
    ],
    "answer": [
      0,
      2,
      3
    ],
    "explanation": "Cilium keeps L3/L4 forwarding, service load balancing and identity policy in eBPF. Envoy is only in the path for HTTP, gRPC and Kafka L7 policy and for ingress features (Ingress, Gateway API, `CiliumEnvoyConfig`). DNS policy is the exception among L7 rules: it uses a lightweight DNS proxy inside the agent.",
    "example": "# see whether Envoy is receiving redirected traffic for an endpoint\nkubectl -n kube-system exec ds/cilium -- cilium-dbg endpoint get 1234 -o jsonpath='{[0].status.policy.realized.l4}'\n# proxy statistics\nkubectl -n kube-system exec ds/cilium -- cilium-dbg status --verbose | grep -A5 Proxy",
    "diagram": "flowchart LR\n    T[\"packet\"] --> E[\"eBPF datapath\"]\n    E -->|\"L3/L4 policy, services\"| F[\"forward in kernel\"]\n    E -->|\"port 53 with dns rule\"| D[\"DNS proxy in agent\"]\n    E -->|\"HTTP, gRPC, Kafka rule or Ingress\"| V[\"Envoy proxy\"]\n    V --> F\n    D --> F",
    "reference": "https://docs.cilium.io/en/stable/security/network/proxy/envoy/",
    "tags": [
      "cca",
      "envoy",
      "l7"
    ],
    "id": "cilium-039",
    "domain": "cilium"
  },
  {
    "difficulty": "medium",
    "type": "boolean",
    "prompt": "Enabling `ciliumEndpointSlice.enabled=true` replaces the per-Pod `CiliumEndpoint` objects with batched `CiliumEndpointSlice` objects so that agents watch fewer resources in large clusters; the `CiliumEndpoint` objects themselves are no longer created.",
    "answer": false,
    "explanation": "With CiliumEndpointSlice enabled the operator aggregates endpoint information into `CiliumEndpointSlice` objects and agents watch those instead of individual `CiliumEndpoint` objects, which reduces API server and watch load at scale. However `CiliumEndpoint` objects are still created for each Pod; the slices are built from them.",
    "example": "cilium install --set ciliumEndpointSlice.enabled=true\nkubectl get ciliumendpointslices\n# NAME          AGE\n# ces-2z8k9...  5m\nkubectl get ciliumendpoints -A | head   # still present per Pod",
    "diagram": "flowchart LR\n    P[\"Pod\"] --> A[\"cilium-agent creates CiliumEndpoint\"]\n    A --> O[\"cilium-operator\"]\n    O -->|\"batches into\"| S[\"CiliumEndpointSlice\"]\n    S -->|\"single watch\"| G[\"agents on all nodes\"]\n    A -.->|\"without CES: every agent watches every CEP\"| G",
    "reference": "https://docs.cilium.io/en/stable/network/kubernetes/ciliumendpointslice/",
    "tags": [
      "cca",
      "scalability",
      "endpoints"
    ],
    "id": "cilium-040",
    "domain": "cilium"
  }
]
